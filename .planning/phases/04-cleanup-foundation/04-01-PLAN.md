---
phase: 04-cleanup-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/app/api/qsa.py
  - Backend/app/api/deputados.py
  - Backend/app/ingest/import_qsa.py
  - Backend/app/scheduler/sync_scheduler.py
  - Backend/tests/test_qsa_ingest.py
  - Backend/tests/test_cnpj_validation.py
  - Backend/app/ingest/import_empresas.py
  - Backend/app/ingest/import_socios.py
autonomous: true
requirements: [CLEANUP-01, CLEANUP-03, CLEANUP-04]
user_setup: []
must_haves:
  truths:
    - "No datetime.utcnow() calls remain in Backend/ — zero deprecation warnings"
    - "import_empresas.py and import_socios.py no longer exist"
    - "importar_qsa_completo() no longer exists in import_qsa.py"
    - "Test suite (86+ tests) passes with no regressions"
    - "processar_csv_socios() has a regression test confirming its return value"
    - "test_cnpj_validation.py keeps 7 CNPJ validation tests untouched"
  artifacts:
    - path: "Backend/app/api/qsa.py"
      provides: "Freshness endpoint with timezone-aware datetime"
      min_lines: 20
      contains: "from datetime import datetime, timezone"
    - path: "Backend/app/api/deputados.py"
      provides: "Deputados endpoints with timezone-aware datetime"
      min_lines: 240
      contains: "from datetime import datetime, timezone"
    - path: "Backend/app/ingest/import_qsa.py"
      provides: "QSA ingest without dead importar_qsa_completo function"
      min_lines: 200
      contains: "def importar_qsa_incremental"
    - path: "Backend/app/scheduler/sync_scheduler.py"
      provides: "Scheduler with timezone-aware datetime"
      contains: "from datetime import datetime, timezone"
    - path: "Backend/tests/test_qsa_ingest.py"
      provides: "Regression test for processar_csv_socios return value"
    - path: "Backend/tests/test_cnpj_validation.py"
      provides: "Remaining CNPJ validation tests (7 tests)"
      contains: "def test_valid_cnpj"
  key_links:
    - from: "Backend/app/api/qsa.py:16"
      to: "datetime.now(timezone.utc)"
      via: "freshness calculation"
    - from: "Backend/app/api/deputados.py:72,173"
      to: "datetime.now(timezone.utc)"
      via: "freshness calculation in list and detail endpoints"
    - from: "Backend/app/ingest/import_qsa.py:187,200"
      to: "datetime.now(timezone.utc)"
      via: "QsaMetadata timestamp creation"
    - from: "Backend/app/scheduler/sync_scheduler.py:46"
      to: "datetime.now(timezone.utc)"
      via: "staleness check for auto-trigger"
    - from: "Backend/tests/test_qsa_ingest.py"
      to: "processar_csv_socios"
      via: "regression test asserts return type/value"
---

<objective>
Fix datetime.utcnow() deprecation across all 4 backend files (6 occurrences), verify the already-fixed `processar_csv_socios()` return bug with a regression test, and remove dead code.

**Purpose:** Eliminate Python 3.12+ deprecation warnings, confirm DQ-01 blocker is permanently fixed, and clean up dead code that clutters the codebase and misleads developers.
**Output:** Clean backend codebase with zero deprecation calls, no dead files, a passing test suite, and a regression test guarding the return value of `processar_csv_socios()`.
</objective>

<execution_context>
@C:/Users/AndreC/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/AndreC/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-cleanup-foundation/04-CONTEXT.md
@.planning/phases/04-cleanup-foundation/04-RESEARCH.md

<interfaces>
<!-- 
Key types and contracts the executor needs for the regression test.
Extracted from Backend/app/ingest/import_qsa.py and Backend/tests/test_qsa_ingest.py.
-->

From Backend/tests/test_qsa_ingest.py:
- Class TestQSAIngest - test group, uses `db_session` fixture from conftest.py
- `test_importar_qsa_metadata_creation` (line 120) - existing test that tests full pipeline
- Pattern: `from app.ingest.import_qsa import <function>`
- Pattern: tempfile.TemporaryDirectory + CSV generation for test fixtures
- Pattern: `processar_csv_socios(csv_path, db_session)` function signature expects (str, Session)

From Backend/conftest.py:
- `db_session` fixture creates SQLite test DB with `Base.metadata.create_all`
- `client` fixture wraps TestClient with dependency override
- Models imported: Empresa, Socio, Relacao, EmpresaCnaeSecundario, Deputado, Partido, QsaMetadata, Config

From Backend/tests/test_cnpj_validation.py:
- Class TestCNPJValidation - has `_validate_cnpj` helper method (duplicated production logic)
- 7 tests that DO NOT reference dead code: test_valid_cnpj, test_invalid_cnpj_format, test_cnpj_with_all_same_digits, test_cnpj_too_short, test_cnpj_too_long, test_cnpj_with_formatting, test_cnpj_checksum_validation, test_validation_failure_logging
- 2 tests that DO reference dead code and MUST be deleted: test_ingestion_rejects_invalid_cnpj, test_empty_cnpj_handling

From Backend/tests/test_qsa_ingest.py:
- Tests to DELETE (reference import_empresas): test_zip_processing (line 10), test_csv_extraction (line 19)
- Tests to KEEP: test_ingest_empresas_socios, test_large_dataset_chunking, test_validation_failures_logged, test_transaction_rollback_on_error, test_temp_files_cleanup, test_ingestion_endpoint, test_upsert_idempotency, test_brazilian_capital_parsing, test_importar_qsa_metadata_creation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix datetime.utcnow() deprecations across 4 backend files + verify CLEANUP-01 with regression test</name>
  <files>
    Backend/app/api/qsa.py
    Backend/app/api/deputados.py
    Backend/app/ingest/import_qsa.py
    Backend/app/scheduler/sync_scheduler.py
    Backend/tests/test_qsa_ingest.py
  </files>
  <read_first>
    Backend/app/api/qsa.py
    Backend/app/api/deputados.py
    Backend/app/ingest/import_qsa.py
    Backend/app/scheduler/sync_scheduler.py
    Backend/tests/test_qsa_ingest.py
    Backend/conftest.py
  </read_first>
  <action>
    Part A — Fix datetime.utcnow() (CLEANUP-03)

    In each of the 4 backend files, make exactly two changes per file:
    1. Add `timezone` to the import: change `from datetime import datetime` to `from datetime import datetime, timezone`
    2. Replace every `datetime.utcnow()` with `datetime.now(timezone.utc)`

    Specific sites per file:
    - Backend/app/api/qsa.py: line 3 (import), line 16 (usage)
    - Backend/app/api/deputados.py: line 67 (import), line 72 (usage), line 167 (import — duplicate import block at line 67), line 173 (usage). Note there are TWO separate import blocks: one at module top (line 3 missing datetime import) and one inline at line 67 `from datetime import datetime`. Fix BOTH.
    - Backend/app/ingest/import_qsa.py: line 5 (import), line 187 (usage), line 200 (usage)
    - Backend/app/scheduler/sync_scheduler.py: line 2 (import), line 46 (usage)

    After making changes, verify with:
    ```powershell
    cd Backend; $env:PYTHONIOENCODING='utf-8'; Get-ChildItem -Recurse -Filter *.py | Select-String -Pattern "datetime\.utcnow" | ForEach-Object { "$($_.Path):$($_.LineNumber)" }
    ```
    This should return zero matches.

    Part B — Verify CLEANUP-01 is fixed + add regression test (CLEANUP-01)

    First, confirm `return total` exists at line 101 of Backend/app/ingest/import_qsa.py (it should already be there).

    Then add a new test method to the existing `TestQSAIngest` class in `Backend/tests/test_qsa_ingest.py` named `test_processar_csv_socios_returns_total` that:
    1. Creates a temporary CSV file with 3+ rows of socio data using `cnpj,cpf_socio,nome_socio,qualificacao` header format
    2. Calls `processar_csv_socios(csv_path, db_session)` directly
    3. Asserts the return value is an int equal to the number of data rows
    4. If test file is empty (no data rows), it should still return 0

    Place this test after `test_validation_failures_logged` and before `test_transaction_rollback_on_error`.

    Run the full test suite to verify no regressions:
    ```powershell
    cd Backend; .\venv\Scripts\activate; if ($?) { python -m pytest tests/ -x --tb=short }
    ```
  </action>
  <acceptance_criteria>
    - Source assertion: `rg "datetime\.utcnow" Backend/ --type py` returns 0 matches
    - Source assertion: Every changed file has `timezone` in the datetime import line
    - Source assertion: Every `datetime.utcnow()` call is replaced with `datetime.now(timezone.utc)`
    - Test command: `cd Backend; .\venv\Scripts\activate; if ($?) { python -m pytest tests/test_qsa_ingest.py::TestQSAIngest::test_processar_csv_socios_returns_total -x --tb=short }` passes
    - Test command: `cd Backend; .\venv\Scripts\activate; if ($?) { python -m pytest tests/ -x --tb=short }` passes
  </acceptance_criteria>
  <verify>
    <automated>cd Backend; .\venv\Scripts\activate; if ($?) { python -m pytest tests/ -x --tb=short }</automated>
  </verify>
  <done>
    All 6 datetime.utcnow() occurrences replaced with datetime.now(timezone.utc). CLEANUP-01 verified fixed with a regression test. Full test suite green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code — delete import_empresas.py, import_socios.py, importar_qsa_completo(), and dependent tests</name>
  <files>
    Backend/app/ingest/import_empresas.py
    Backend/app/ingest/import_socios.py
    Backend/app/ingest/import_qsa.py
    Backend/tests/test_qsa_ingest.py
    Backend/tests/test_cnpj_validation.py
  </files>
  <read_first>
    Backend/app/ingest/import_empresas.py
    Backend/app/ingest/import_socios.py
    Backend/app/ingest/import_qsa.py (lines 239-258 specifically)
    Backend/tests/test_qsa_ingest.py (lines 10-27 specifically)
    Backend/tests/test_cnpj_validation.py (lines 49-73 specifically)
  </read_first>
  <action>
    Perform the following removals in order:

    1. DELETE `Backend/app/ingest/import_empresas.py` — entire file (40 lines, dead code superseded by import_qsa.py's processar_csv_empresas)
    2. DELETE `Backend/app/ingest/import_socios.py` — entire file (40 lines, dead code superseded by import_qsa.py's processar_csv_socios)
    3. In `Backend/app/ingest/import_qsa.py`, remove the entire `importar_qsa_completo()` function (lines 239-258 inclusive). Do NOT remove `baixar_qsa()` or `extrair_csvs()` — those are still used by `importar_qsa_incremental()`.
    4. In `Backend/tests/test_qsa_ingest.py`, delete these two test methods (they reference deleted import_empresas):
       - `test_zip_processing` (lines 10-17)
       - `test_csv_extraction` (lines 19-27)
       Keep all other 7 tests intact.
    5. In `Backend/tests/test_cnpj_validation.py`, delete these two test methods (they reference deleted import_empresas):
       - `test_ingestion_rejects_invalid_cnpj` (lines 49-61)
       - `test_empty_cnpj_handling` (lines 62-73)
       Keep all 7 other tests: test_valid_cnpj, test_invalid_cnpj_format, test_cnpj_with_all_same_digits, test_cnpj_too_short, test_cnpj_too_long, test_cnpj_with_formatting, test_cnpj_checksum_validation, test_validation_failure_logging. These test `_validate_cnpj` independently and do NOT depend on dead code.

    After all removals, run the full test suite:
    ```powershell
    cd Backend; .\venv\Scripts\activate; if ($?) { python -m pytest tests/ -x --tb=short }
    ```

    Then verify:
    - `Test-Item "Backend/app/ingest/import_empresas.py"` should throw (file gone)
    - `Test-Item "Backend/app/ingest/import_socios.py"` should throw (file gone)
    - `Select-String -Path "Backend/app/ingest/import_qsa.py" -Pattern "def importar_qsa_completo"` should return 0 matches
  </action>
  <acceptance_criteria>
    - CLI assertion: `Test-Path "Backend/app/ingest/import_empresas.py"` → False
    - CLI assertion: `Test-Path "Backend/app/ingest/import_socios.py"` → False
    - Source assertion: `importar_qsa_completo` does not appear in `Backend/app/ingest/import_qsa.py`
    - Source assertion: `test_zip_processing` does not appear in `Backend/tests/test_qsa_ingest.py`
    - Source assertion: `test_csv_extraction` does not appear in `Backend/tests/test_qsa_ingest.py`
    - Source assertion: `test_ingestion_rejects_invalid_cnpj` does not appear in `Backend/tests/test_cnpj_validation.py`
    - Source assertion: `test_empty_cnpj_handling` does not appear in `Backend/tests/test_cnpj_validation.py`
    - Source assertion: `test_valid_cnpj` still exists in `Backend/tests/test_cnpj_validation.py`
    - Test command: `cd Backend; .\venv\Scripts\activate; if ($?) { python -m pytest tests/ -x --tb=short }` passes
  </acceptance_criteria>
  <verify>
    <automated>cd Backend; .\venv\Scripts\activate; if ($?) { python -m pytest tests/ -x --tb=short }</automated>
  </verify>
  <done>
    import_empresas.py and import_socios.py deleted. importar_qsa_completo() removed. 4 dead-code tests removed. All remaining 7 CNPJ validation tests pass. Full test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend file system | Dead code removal and datetime fixes operate entirely within the existing backend codebase. No new trust boundaries are introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Backend/tests/ | mitigate | Run `pytest tests/ -x` after every task to catch test breakage from dead code removal |
| T-04-SC | Supply Chain | — | accept | No new packages installed — all work is modifying existing code or deleting dead files |
</threat_model>

<verification>
1. Full test suite passes: `cd Backend; .\venv\Scripts\activate; if ($?) { python -m pytest tests/ -x --tb=short }`
2. Zero datetime.utcnow() calls remain: `Select-String -Path "Backend" -Pattern "datetime\.utcnow" -Recurse` returns zero matches
3. Dead files confirmed gone: `Test-Path` returns False for both import_empresas.py and import_socios.py
4. importar_qsa_completo function removed: grep for `def importar_qsa_completo` in import_qsa.py returns 0 matches
</verification>

<success_criteria>
- [ ] All 6 datetime.utcnow() occurrences replaced with datetime.now(timezone.utc)
- [ ] Zero datetime.utcnow() calls in Backend/ (verified by grep)
- [ ] import_empresas.py and import_socios.py deleted
- [ ] importar_qsa_completo() removed from import_qsa.py
- [ ] 4 dead-code tests deleted (2 from test_qsa_ingest.py, 2 from test_cnpj_validation.py)
- [ ] 7 CNPJ validation tests remain in test_cnpj_validation.py
- [ ] Regression test for processar_csv_socios return value added to test_qsa_ingest.py
- [ ] Full test suite passes (pytest tests/ -x)
</success_criteria>

<output>
Create `.planning/phases/04-cleanup-foundation/04-01-SUMMARY.md` when done
</output>
