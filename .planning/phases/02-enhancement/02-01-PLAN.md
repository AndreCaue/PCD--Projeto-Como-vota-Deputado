---
phase: 02-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/app/models/empresa.py
  - Backend/app/models/qsa_metadata.py
  - Backend/app/models/config.py
  - Backend/app/models/__init__.py
  - Backend/app/ingest/migrate_schema.py
  - Backend/conftest.py
  - Backend/tests/test_qsa_flags.py
autonomous: true
requirements:
  - CONF-03
  - CONF-04
  - INF-04
  - DQ-01
user_setup: []

must_haves:
  truths:
    - "Relacao model has alta_exposicao and via_conjuge boolean columns"
    - "Empresa model has capital_social Float column (nullable)"
    - "QsaMetadata and Config models exist as SQLAlchemy tables"
    - "Schema migration is idempotent (safe to run multiple times)"
    - "Database indices exist for relacoes(deputado_id, cnpj) and relacoes(alta_exposicao)"
    - "Test fixtures exist for sample_empresa_with_capital, sample_qsa_metadata, sample_config"
    - "Test file test_qsa_flags.py exists with placeholder tests for flag computation"
  artifacts:
    - path: "Backend/app/models/qsa_metadata.py"
      provides: "QsaMetadata model with last_import_at, status, row_count columns"
      min_lines: 15
    - path: "Backend/app/models/config.py"
      provides: "Config model with key/value pair table"
      min_lines: 10
    - path: "Backend/app/models/empresa.py"
      provides: "Relacao model with alta_exposicao and via_conjuge columns; Empresa model with capital_social column"
      contains: "alta_exposicao"
    - path: "Backend/app/ingest/migrate_schema.py"
      provides: "Idempotent schema migration + index creation"
      exports:
        - "ensure_schema"
    - path: "Backend/conftest.py"
      provides: "Test fixtures for QSA flag tests"
      contains: "sample_empresa_with_capital"
  key_links:
    - from: "Backend/app/ingest/migrate_schema.py"
      to: "Backend/app/models/empresa.py"
      via: "ALTER TABLE ADD COLUMN alta_exposicao"
      pattern: "ALTER TABLE relacoes ADD COLUMN alta_exposicao"
    - from: "Backend/app/ingest/migrate_schema.py"
      to: "Backend/app/models/qsa_metadata.py"
      via: "Base.metadata.create_all with QsaMetadata.__table__"
      pattern: "QsaMetadata.__table__"
---

# Phase 2 — Plan 01: Foundation — Data Model + Schema Migration + Test Infrastructure

## Goal

Create the data model extensions (new columns, new tables) and schema migration script that all subsequent Phase 2 plans depend on. Also establish test fixtures for the new entities.

## Purpose

This plan provides the storage layer contracts for `alta_exposicao`, `via_conjuge`, `capital_social`, `qsa_metadata`, and the `config` table. Every downstream plan reads or writes through these models, so they must exist before any service/API work begins.

## Execution Context

@C:/Users/AndreC/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/AndreC/.config/opencode/get-shit-done/templates/summary.md

## Context

### Source of Truth Files (read first before implementation)

<read_first>
- Backend/app/models/empresa.py — current model to extend (lines 1-36)
- Backend/app/models/__init__.py — barrel exports to update (lines 1-5)
- Backend/app/models/base.py — base model pattern (line 1)
- Backend/app/models/votacao.py (SyncLog at lines 55-63) — analog for QsaMetadata
- Backend/app/database.py — engine, SessionLocal, Base imports
- Backend/app/create_db.py — existing schema init pattern
- Backend/conftest.py — existing test fixtures to extend
</read_first>

<interfaces>
From Backend/app/models/empresa.py (current state):
```python
class Relacao(Base):
    __tablename__ = "relacoes"
    id = Column(Integer, primary_key=True, autoincrement=True)
    deputado_id = Column(String, index=True)
    cnpj = Column(String, index=True)
    tipo_relacao = Column(String)  # cpf_match, nome_match
    score_confianca = Column(Integer)
    relationship_type = Column(Boolean, index=True)
    origem = Column(String, nullable=True)
    criado_em = Column(DateTime, server_default=func.now())
```

Inspected table columns in relacoes (from existing tests):
- Column names from `PRAGMA table_info(relacoes)`: id, deputado_id, cnpj, tipo_relacao, score_confianca, relationship_type, origem, criado_em

From Backend/app/create_db.py:
- Pattern: `from .database import Base, engine` + `Base.metadata.create_all(bind=engine)`
</interfaces>

### Key Decisions (from CONTEXT.md)

- **D-07**: Add `alta_exposicao` and `via_conjuge` as boolean columns on Relacao model
- **D-08**: `via_conjuge` = True when `tipo_relacao` is `nome_match`
- **D-09**: `alta_exposicao` threshold stored in Config table for runtime adjustability
- **D-10**: Dedicated QSA metadata table (`qsa_metadata`) with `last_import_at`, `status`, `row_count`
- **D-13**: Staleness threshold: 45 days (configurable via config table, but default stored for staleness flag)
- **D-03**: Keep field name as `score_confianca` (Portuguese)

<tasks>

<task type="auto">
<name>Task 1: Extend models — add columns to Relacao and Empresa, create QsaMetadata and Config models</name>

<files>
- Backend/app/models/empresa.py
- Backend/app/models/qsa_metadata.py
- Backend/app/models/config.py
- Backend/app/models/__init__.py
</files>

<read_first>
- Backend/app/models/empresa.py (current — lines 1-36)
- Backend/app/models/__init__.py (current — lines 1-5)
- Backend/app/models/base.py (line 1 — `from ..database import Base`)
- Backend/app/models/votacao.py — SyncLog model (lines 55-63) as analog for QsaMetadata
- Backend/app/database.py — imports pattern
</read_first>

<action>
Modify `empresa.py`:

1. Import `Float` in the existing sqlalchemy import line at the top. The current line is:
   ```
   from sqlalchemy import Column, String, Integer, Boolean, DateTime, func
   ```
   Add `Float` after `DateTime`.

2. On the `Empresa` model (before `Socio` class), add a new column after `situacao`:
   - `capital_social = Column(Float, nullable=True)`

3. On the `Relacao` model (after `criado_em`), add two new columns:
   - `alta_exposicao = Column(Boolean, default=False, index=True)`
   - `via_conjuge = Column(Boolean, default=False)`

Create `qsa_metadata.py` (new):
- Import: `from sqlalchemy import Column, Integer, String, DateTime, func`
- Import: `from .base import Base`
- Create `QsaMetadata` class with `__tablename__ = "qsa_metadata"`
- Columns: `id` (Integer PK autoincrement), `last_import_at` (DateTime not nullable), `status` (String not nullable), `row_count` (Integer default=0), `error_message` (String nullable), `criado_em` (DateTime server_default=func.now())
- Follow the SyncLog pattern from `votacao.py:55-63` exactly

Create `config.py` (new):
- Import: `from sqlalchemy import Column, Integer, String`
- Import: `from .base import Base`
- Create `Config` class with `__tablename__ = "config"`
- Columns: `id` (Integer PK autoincrement), `key` (String unique not nullable), `value` (String not nullable)

Update `__init__.py`:
- Add `from .empresa import Empresa, Socio, Relacao` (already exists in create_db.py, add to __init__.py)
- Add `from .qsa_metadata import QsaMetadata`
- Add `from .config import Config`
- Keep existing imports — append new ones after the last existing line
</action>

<verify>
<automated>cd Backend && python -c "from app.models.empresa import Empresa, Relacao; from app.models.qsa_metadata import QsaMetadata; from app.models.config import Config; print('Models imported OK')"</automated>
</verify>

<acceptance_criteria>
- `Empresa` model has `capital_social = Column(Float, nullable=True)` attribute after `situacao`
- `Relacao` model has `alta_exposicao = Column(Boolean, default=False, index=True)` after `criado_em`
- `Relacao` model has `via_conjuge = Column(Boolean, default=False)` after `alta_exposicao`
- `QsaMetadata` class exists with columns: id, last_import_at, status, row_count, error_message, criado_em
- `Config` class exists with columns: id, key (unique), value
- `__init__.py` exports `QsaMetadata` and `Config`
- `python -c` import test passes without ImportError
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 2: Create idempotent schema migration script with index creation</name>

<files>
- Backend/app/ingest/migrate_schema.py
</files>

<read_first>
- Backend/app/create_db.py — existing DB init pattern (lines 1-17)
- Backend/app/database.py — engine, Base imports
- Backend/app/models/empresa.py — to confirm column names
- Backend/app/models/qsa_metadata.py — to confirm column names (just created in Task 1)
</read_first>

<action>
Create `Backend/app/ingest/migrate_schema.py` with function `ensure_schema()`:

1. Import `logging`, `inspect`, `text` from sqlalchemy, and local modules.
2. Use `inspect(engine)` to get existing tables and their columns.
3. Create new tables (QsaMetadata, Config) idempotently via `Base.metadata.create_all(bind=engine, tables=[QsaMetadata.__table__, Config.__table__])`.
4. For `relacoes` table — add `alta_exposicao` and `via_conjuge` columns with PRAGMA guard:
   - Use `inspector.get_columns("relacoes")` to get existing column names
   - If `"alta_exposicao"` not in existing cols: `ALTER TABLE relacoes ADD COLUMN alta_exposicao BOOLEAN DEFAULT 0`
   - If `"via_conjuge"` not in existing cols: `ALTER TABLE relacoes ADD COLUMN via_conjuge BOOLEAN DEFAULT 0`
5. For `empresas` table — add `capital_social` column:
   - If `"capital_social"` not in existing cols: `ALTER TABLE empresas ADD COLUMN capital_social FLOAT`
6. Create indices using `conn.execute(text(...))`:
   - `CREATE INDEX IF NOT EXISTS idx_relacoes_deputado_cnpj ON relacoes(deputado_id, cnpj)`
   - `CREATE INDEX IF NOT EXISTS idx_relacoes_alta_exposicao ON relacoes(alta_exposicao)`
7. All ALTER TABLE and CREATE INDEX operations must be wrapped in a single connection context
8. Follow the error handling pattern from existing `import_qsa.py`: try/except with logging, rollback on failure

Use `engine.connect()` with `conn.execute()` for raw SQL, not `engine.execute()` (deprecated in SQLAlchemy 2.x).

Note: The `Base` import should come from `app.database` like in `create_db.py`. Use `from ..database import engine, Base`.
</action>

<verify>
<automated>cd Backend && python -c "from app.ingest.migrate_schema import ensure_schema; print('migrate_schema imported OK')"</automated>
</verify>

<acceptance_criteria>
- `migrate_schema.py` exists with `ensure_schema()` function
- Function uses `inspect(engine)` for idempotent PRAGMA-based column guards
- Function creates `alta_exposicao` and `via_conjuge` on relacoes with ALTER TABLE ADD COLUMN
- Function creates `capital_social` on empresas with ALTER TABLE ADD COLUMN
- Function creates `idx_relacoes_deputado_cnpj` and `idx_relacoes_alta_exposicao` indices
- Function uses `CREATE INDEX IF NOT EXISTS` for index creation
- Python import test passes without ImportError
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 3: Add test fixtures and create test file for flag computation</name>

<files>
- Backend/conftest.py
- Backend/tests/test_qsa_flags.py
</files>

<read_first>
- Backend/conftest.py (current — lines 1-109)
- Backend/models/empresa.py (just modified in Task 1)
- Backend/app/models/qsa_metadata.py (just created in Task 1)
- Backend/tests/test_qsa_matching.py (current — lines 1-70) — existing test pattern
</read_first>

<action>
Extend `conftest.py`:

1. Add import for `Float` if not already there from sqlalchemy (or skip — it's not needed in conftest).
2. Add import for `QsaMetadata` from app.models.qsa_metadata and `Config` from app.models.config.

3. Add three new fixtures:

   **`sample_empresa_with_capital`**: Follow `sample_empresa` fixture pattern but set `capital_social=2000000.00` (above 1M threshold for alta_exposicao). Also change `cnpj` to `"99887766000199"` (different from sample_empresa to allow both to coexist).

   **`sample_qsa_metadata`**: Creates a QsaMetadata row with:
   - `last_import_at=datetime.datetime(2026, 1, 1)` (over 45 days ago — stale)
   - `status="success"`
   - `row_count=1000`
   - Import `datetime` at top of conftest.py if not already there.

   **`sample_config`**: Creates a Config row with `key="alta_exposicao_threshold"`, `value="1000000.0"`.

4. Add `import datetime` to the top imports.

Create `tests/test_qsa_flags.py`:

1. Use `pytest` and `TestClient` imports, following the existing test file pattern.
2. Create class `TestQSAFlags` with these test stubs:

   `test_alta_exposicao_model_column` — Verifies the Relacao model has `alta_exposicao` attribute:
   ```python
   def test_alta_exposicao_model_column(self, db_session):
       from app.models.empresa import Relacao
       assert hasattr(Relacao, "alta_exposicao")
   ```

   `test_via_conjuge_model_column` — Verifies the Relacao model has `via_conjuge` attribute:
   ```python
   def test_via_conjuge_model_column(self, db_session):
       from app.models.empresa import Relacao
       assert hasattr(Relacao, "via_conjuge")
   ```

   `test_capital_social_model_column` — Verifies the Empresa model has `capital_social` attribute:
   ```python
   def test_capital_social_model_column(self, db_session):
       from app.models.empresa import Empresa
       assert hasattr(Empresa, "capital_social")
   ```

   `test_qsa_metadata_columns` — Verifies QsaMetadata has required columns:
   ```python
   def test_qsa_metadata_columns(self, db_session, sample_qsa_metadata):
       from app.models.qsa_metadata import QsaMetadata
       meta = db_session.query(QsaMetadata).first()
       assert meta.last_import_at is not None
       assert meta.status == "success"
       assert meta.row_count == 1000
   ```

   `test_config_model` — Verifies Config model works:
   ```python
   def test_config_model(self, db_session, sample_config):
       from app.models.config import Config
       config = db_session.query(Config).first()
       assert config.key == "alta_exposicao_threshold"
       assert config.value == "1000000.0"
   ```

3. All tests must be runnable with `pytest tests/test_qsa_flags.py -x -q`.
</action>

<verify>
<automated>cd Backend && pytest tests/test_qsa_flags.py -x -q</automated>
</verify>

<acceptance_criteria>
- `conftest.py` has `sample_empresa_with_capital` fixture with `capital_social=2000000.00`
- `conftest.py` has `sample_qsa_metadata` fixture with `last_import_at` >45 days stale
- `conftest.py` has `sample_config` fixture with `alta_exposicao_threshold` key
- `test_qsa_flags.py` has 5 tests: alta_exposicao column, via_conjuge column, capital_social column, qsa_metadata columns, config model
- All 5 tests pass when running `pytest tests/test_qsa_flags.py -x -q`
</acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL execution in schema migration | Raw SQL executed via conn.execute(text(...)) for DDL operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `migrate_schema.py` raw SQL | mitigate | All ALTER TABLE commands use PRAGMA table_info guards to prevent errors on re-run. No user-supplied data enters these statements. |
| T-02-02 | Information Disclosure | New model columns (capital_social has financial data) | accept | Capital social is public data from Receita Federal QSA dataset — no business-confidential information. |
| T-02-SC | Tampering | package installs | mitigate | No new packages added in this plan. All dependencies already in requirements.txt (verified by RESEARCH.md package audit). |

</threat_model>

<verification>
1. `cd Backend && python -c "from app.models.empresa import Empresa, Relacao; from app.models.qsa_metadata import QsaMetadata; from app.models.config import Config; print('Models OK')"`
2. `cd Backend && python -c "from app.ingest.migrate_schema import ensure_schema; print('Migration OK')"`
3. `cd Backend && pytest tests/test_qsa_flags.py -x -q` — all 5 tests pass
4. `cd Backend && pytest tests/ -x -q` — all existing tests still pass (no regressions)
</verification>

<success_criteria>
- All model changes committed and importable without errors
- Schema migration script creates new columns and tables idempotently
- Test infrastructure is in place for downstream plan verification
- Existing test suite still passes (no regression from model changes)
</success_criteria>
