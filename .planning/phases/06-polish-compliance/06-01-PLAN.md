---
phase: 06-polish-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Frontend/components/fiscalizacao/ScoreBreakdownBar.tsx
  - Frontend/components/fiscalizacao/CnaeLabel.tsx
  - Frontend/components/fiscalizacao/QsaRelationshipCard.tsx
  - Frontend/components/fiscalizacao/QsaInlineSection.tsx
  - Frontend/components/fiscalizacao/index.tsx
autonomous: true
requirements: [QSA-12, QSA-13]
user_setup: []

must_haves:
  truths:
    - "User can see a colored 50/30/20 breakdown bar showing why each relationship's score is what it is"
    - "User can read human-readable CNAE category descriptions alongside raw codes on each relationship"
    - "Zero-score relationships show 'Sem fatores de conflito' instead of an empty bar"
    - "Conflict-class CNAEs (41204, 70204, 73190, 86101) appear in amber/red on both card and profile inline section"
    - "Bar segments use green/amber/blue with legend labels matching the Capital/CNAE/CPF scoring formula"
  artifacts:
    - path: "Frontend/components/fiscalizacao/ScoreBreakdownBar.tsx"
      provides: "CSS-only 50/30/20 segmented bar with active/inactive segment states and legend"
      min_lines: 50
    - path: "Frontend/components/fiscalizacao/CnaeLabel.tsx"
      provides: "CNAE description + code display with conflict-class color highlighting"
      min_lines: 30
    - path: "Frontend/components/fiscalizacao/QsaRelationshipCard.tsx"
      provides: "ScoreBreakdownBar and CnaeLabel integrated into expanded detail section (lines 109-141)"
      contains: "ScoreBreakdownBar"
    - path: "Frontend/components/fiscalizacao/QsaInlineSection.tsx"
      provides: "CnaeLabel in each relationship row on deputy profile page"
      contains: "CnaeLabel"
  key_links:
    - from: "QsaRelationshipCard.tsx"
      to: "ScoreBreakdownBar.tsx"
      via: "import in card expanded section"
      pattern: "import.*ScoreBreakdownBar"
    - from: "QsaRelationshipCard.tsx"
      to: "CnaeLabel.tsx"
      via: "import and render next to CNAE code"
      pattern: "import.*CnaeLabel"
    - from: "QsaInlineSection.tsx"
      to: "CnaeLabel.tsx"
      via: "import and render in relationship row"
      pattern: "import.*CnaeLabel"
---

<objective>
**Deliverable:** Score breakdown visualization (QSA-12) + CNAE human-readable labels (QSA-13) inside existing relationship card expanded section and deputy profile inline section.

**Purpose:** Users need to understand WHY a relationship score is 20, 50, or 100 — not just see a number. CNAE codes (e.g. "41204") are meaningless to non-experts; showing the description (e.g. "Construção de edifícios") makes the data accessible.

**Output:** Two new components (ScoreBreakdownBar, CnaeLabel), modifications to QsaRelationshipCard and QsaInlineSection to consume them, barrel exports in index.tsx.
</objective>

<execution_context>
@C:/Users/AndreC/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/AndreC/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-compliance/06-CONTEXT.md
@.planning/phases/06-polish-compliance/06-RESEARCH.md

<interfaces>
From Frontend/services/api.ts:240-257 (RelacaoDetalhada type):
```typescript
export interface RelacaoDetalhada {
  id: number;
  cnpj: string;
  relationship_type: boolean | null;   // false=cpf_match, true=name_match
  score_conflito: number;              // 0-100 total
  conflito_interesse: boolean;
  alta_exposicao: boolean;             // capital > 1M → +50
  empresa: {
    razao_social: string;
    capital_social: number | null;
    cnae_principal: string | null;     // e.g. "41204"
    cnae_descricao: string | null;     // e.g. "Construção de edifícios"
  };
}
```

From Frontend/components/fiscalizacao/index.tsx:
```typescript
// Current exports — add ScoreBreakdownBar and CnaeLabel
export { ScoreBreakdownBar } from "./ScoreBreakdownBar";
export { CnaeLabel } from "./CnaeLabel";
```

From Frontend/components/fiscalizacao/QsaRelationshipCard.tsx:109-141:
```typescript
// Expanded section detail row per relationship — replace score text and cnae_principal span
{relacoes.map((rel) => (
  <div key={rel.id} className="...">
    <p className="...">{rel.empresa.razao_social}</p>
    <p className="...">{formatCnpj(rel.cnpj)}</p>
    <div className="flex items-center gap-3 mt-1">
      {/* capital value stays */}
      <span className="..."> {/* ← REPLACE this score text with ScoreBreakdownBar */ }
        {rel.score_conflito}/100
      </span>
      {/* ← REPLACE this raw CNAE code with CnaeLabel */}
      <span className="...">{rel.empresa.cnae_principal}</span>
    </div>
    <div className="flex flex-wrap gap-2 mt-2">
      {/* badge row stays */ }
    </div>
  </div>
))}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScoreBreakdownBar and CnaeLabel components</name>
  <files>
    Frontend/components/fiscalizacao/ScoreBreakdownBar.tsx
    Frontend/components/fiscalizacao/CnaeLabel.tsx
    Frontend/components/fiscalizacao/index.tsx
  </files>

  <read_first>
    Frontend/components/fiscalizacao/ConflictBadge.tsx (existing dark-theme card styling patterns)
    Frontend/components/fiscalizacao/ExposureIndicator.tsx (existing inline indicator pattern)
    Frontend/services/api.ts (RelacaoDetalhada type)
    .planning/phases/06-polish-compliance/06-CONTEXT.md (D-01 through D-08)
    .planning/phases/06-polish-compliance/06-RESEARCH.md (Pattern 1 score derivation, Pattern 2 bar CSS, Pattern 3 CNAE code example)
  </read_first>

  <action>
    Create ScoreBreakdownBar.tsx:
    - Export `function ScoreBreakdownBar` with props: `scoreConflito: number`, `altaExposicao: boolean`, `relationshipType: boolean | null`
    - Derive factors per Pattern 1: capitalScore = altaExposicao ? 50 : 0; cpfScore = relationshipType === false ? 20 : 0; cnaeScore = scoreConflito - capitalScore - cpfScore
    - WHEN scoreConflito === 0: render `<p className="text-xs text-gray-500 italic">Sem fatores de conflito</p>` — per D-05
    - WHEN scoreConflito > 0: render a flex container with three div segments at 50%/30%/20% width
    - Active segment color: Capital=bg-emerald-500, CNAE=bg-amber-500, CPF=bg-blue-500 — per D-03
    - Inactive segment color: bg-gray-700/50 for all three
    - Container: `flex h-3 w-full rounded-full overflow-hidden bg-gray-800`
    - Legend below bar: flex row with three span items, each showing colored swatch + label text
    - Legend labels exactly: "Capital (50pts)", "CNAE (30pts)", "CPF (20pts)" — per D-02
    - Legend swatch: `<span className="w-2 h-2 rounded-sm bg-{color}-500" />`
    - Legend text: `text-[10px] text-gray-400`
    - CSS-only: no canvas, no JS animation — per D-04
    - Use `"use client"` if using state/effects (not needed for pure presentational component, but project convention is to add it)
    - Do NOT add inline implementations in <action> — use the described structure above and research references

    Create CnaeLabel.tsx:
    - Export `function CnaeLabel` with props: `cnaePrincipal: string | null`, `cnaeDescricao: string | null`
    - Define `CONFLICT_CNAE_CLASSES = ["41204", "70204", "73190", "86101"]` as a module-level const array — per Research Pitfall 2, single source of truth
    - Extract cnaeClass from cnaePrincipal.slice(0, 5)
    - If cnaePrincipal is null/empty, return null
    - Render: description text + raw code text in a flex row
    - Conflict CNAE: description=text-amber-300, code=text-amber-400 font-mono
    - Non-conflict CNAE: description=text-gray-300, code=text-gray-500 font-mono
    - Code text: `text-xs` for both
    - D-08: no separate risk-category grouping beyond color treatment
    - Show both description AND code — per D-06

    Update index.tsx:
    - Add `export { ScoreBreakdownBar } from "./ScoreBreakdownBar";`
    - Add `export { CnaeLabel } from "./CnaeLabel";`
  </action>

  <verify>
    <automated>
      # Verify files created
      Test-Path -LiteralPath "Frontend/components/fiscalizacao/ScoreBreakdownBar.tsx"
      Test-Path -LiteralPath "Frontend/components/fiscalizacao/CnaeLabel.tsx"
      
      # Verify exports added to barrel
      Select-String -Path "Frontend/components/fiscalizacao/index.tsx" -Pattern "ScoreBreakdownBar"
      Select-String -Path "Frontend/components/fiscalizacao/index.tsx" -Pattern "CnaeLabel"

      # Verify frontend builds
      cd Frontend; if ($?) { npm run build --no-lint 2>&1 | tail -20 }
    </automated>
  </verify>

  <acceptance_criteria>
    - Frontend/components/fiscalizacao/ScoreBreakdownBar.tsx exists with exported function ScoreBreakdownBar
    - Component derives capitalScore=50 from altaExposicao=true, cpfScore=20 from relationship_type=false
    - Zero-score path renders text "Sem fatores de conflito"
    - Active segments use bg-emerald-500, bg-amber-500, bg-blue-500
    - Frontend/components/fiscalizacao/CnaeLabel.tsx exists with exported function CnaeLabel
    - CONFLICT_CNAE_CLASSES array contains ["41204", "70204", "73190", "86101"]
    - Conflict CNAE uses text-amber-300/text-amber-400; non-conflict uses text-gray-300/text-gray-500
    - index.tsx exports both new components
    - `npm run build` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integrate ScoreBreakdownBar and CnaeLabel into QsaRelationshipCard expanded section</name>
  <files>
    Frontend/components/fiscalizacao/QsaRelationshipCard.tsx
  </files>

  <read_first>
    Frontend/components/fiscalizacao/QsaRelationshipCard.tsx (lines 109-141 — the expanded section detail row)
    Frontend/components/fiscalizacao/ScoreBreakdownBar.tsx (new component, just created in Task 1)
    Frontend/components/fiscalizacao/CnaeLabel.tsx (new component, just created in Task 1)
  </read_first>

  <action>
    In QsaRelationshipCard.tsx expanded section (the `.map((rel) => (` block at lines 109-141):

    1. Add imports for ScoreBreakdownBar and CnaeLabel at the top of the file (alongside existing imports from "./ConflictBadge" etc.)

    2. Locate the `<div className="flex items-center gap-3 mt-1">` block (lines 115-131) that contains:
       - Capital social display (keep)
       - `{rel.score_conflito}/100` score text — REPLACE entirely
       - `<span className="text-xs font-mono text-gray-500">{rel.empresa.cnae_principal}</span>` — REPLACE entirely
       - Other elements (badge row at line 132) — keep as-is

    3. In place of the score text ("{rel.score_conflito}/100"), render:
       - D-01 placement: "below capital value and existing badges" — so put ScoreBreakdownBar AFTER the capital value and AFTER the flex row with badges
       - Actually per D-01: "below capital value and existing badges" — the breakdown bar goes below the capital+badge row
       - Structure the expanded detail section as:
         ```
         company name
         CNPJ
         <flex row> capital_value | Cnaelabel        ← capital + CNAE label on same line
         <badge row> ConflictBadge | MatchType | Exposure | Spouse
         <ScoreBreakdownBar>                                 ← full-width below badges
         ```

    4. Remove the colored score span (`text-green-400 / text-amber-400 / text-red-400` with `{rel.score_conflito}/100`). Per the research, this is replaced by ScoreBreakdownBar.

    5. Replace the raw CNAE code span with `<CnaeLabel cnaePrincipal={rel.empresa.cnae_principal} cnaeDescricao={rel.empresa.cnae_descricao} />`

    6. Keep all existing event handlers, loading/error/empty states, and the outer card structure unchanged.

    **Per D-01 clarification:** The score breakdown bar goes in the expanded detail section BELOW the capital value and existing badge row, not inline with them. This ensures the bar has full width and doesn't compress the badge layout.
  </action>

  <verify>
    <automated>
      # Verify imports added
      Select-String -Path "Frontend/components/fiscalizacao/QsaRelationshipCard.tsx" -Pattern "import.*ScoreBreakdownBar"
      Select-String -Path "Frontend/components/fiscalizacao/QsaRelationshipCard.tsx" -Pattern "import.*CnaeLabel"
      
      # Verify ScoreBreakdownBar rendered
      Select-String -Path "Frontend/components/fiscalizacao/QsaRelationshipCard.tsx" -Pattern "ScoreBreakdownBar"
      
      # Verify CnaeLabel rendered
      Select-String -Path "Frontend/components/fiscalizacao/QsaRelationshipCard.tsx" -Pattern "CnaeLabel"
      
      # Verify old score text removed (should NOT find the pattern)
      $result = Select-String -Path "Frontend/components/fiscalizacao/QsaRelationshipCard.tsx" -Pattern "score_conflito.*/100"
      if ($result) { Write-Error "Old score text still present" } else { Write-Output "Old score text removed OK" }
      
      # Verify old CNAE span pattern removed (should NOT find the standalone span)
      $result2 = Select-String -Path "Frontend/components/fiscalizacao/QsaRelationshipCard.tsx" -Pattern "cnae_principal.*text-gray-500"
      if ($result2) { Write-Error "Old CNAE span still present" } else { Write-Output "Old CNAE span removed OK" }
      
      # Build check
      cd Frontend; if ($?) { npm run build --no-lint 2>&1 | tail -10 }
    </automated>
  </verify>

  <acceptance_criteria>
    - ScoreBreakdownBar imported in QsaRelationshipCard.tsx
    - CnaeLabel imported in QsaRelationshipCard.tsx
    - The colored score span (`score_conflito}/100` with green/amber/red conditional classes) is removed
    - The raw CNAE code span (`cnae_principal`) is replaced with CnaeLabel component
    - ScoreBreakdownBar rendered below the badge row in the expanded section
    - CnaeLabel rendered adjacent to capital value in the expanded section
    - All existing loading, error, and empty states preserved
    - `npm run build` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add CnaeLabel to QsaInlineSection deputy profile</name>
  <files>
    Frontend/components/fiscalizacao/QsaInlineSection.tsx
  </files>

  <read_first>
    Frontend/components/fiscalizacao/QsaInlineSection.tsx (lines 102-121 — relationship rows in deputy profile section)
    Frontend/components/fiscalizacao/CnaeLabel.tsx (new component)
  </read_first>

  <action>
    Per Research Open Question 2 (recommendation: add CNAE labels to QsaInlineSection for consistency):

    1. Add import for CnaeLabel at the top of QsaInlineSection.tsx (alongside existing ConflictBadge, MatchTypeBadge, ExposureIndicator, SpouseDisclosure imports)

    2. In the relationship row block (lines 104-119), after the capital social paragraph (line 108-112), add the CnaeLabel:
       - The current structure per relationship is: company name → CNPJ → capital_social → badge row
       - Insert `<CnaeLabel cnaePrincipal={rel.empresa.cnae_principal} cnaeDescricao={rel.empresa.cnae_descricao} />` between the capital_social display and the badge row
       - Use a `mt-1` margin to space it from the capital value

    3. Keep all existing structure and event handlers unchanged.
  </action>

  <verify>
    <automated>
      Select-String -Path "Frontend/components/fiscalizacao/QsaInlineSection.tsx" -Pattern "import.*CnaeLabel"
      Select-String -Path "Frontend/components/fiscalizacao/QsaInlineSection.tsx" -Pattern "<CnaeLabel"
      cd Frontend; if ($?) { npm run build --no-lint 2>&1 | tail -10 }
    </automated>
  </verify>

  <acceptance_criteria>
    - CnaeLabel imported in QsaInlineSection.tsx
    - CnaeLabel rendered in each relationship row, between capital_social display and badge row
    - All existing structure and loading/error/empty states preserved
    - `npm run build` succeeds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component→DOM | New components render score/CNAE data as DOM elements — no user input, no XSS vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | ScoreBreakdownBar | accept | Bar renders from existing API response fields — no new data path or user input. Score factor derivation is client-side only; if backend formula changes, bar may show incorrect segments but this is a correctness issue, not a security issue. |
</threat_model>

<verification>
- All component files exist: ScoreBreakdownBar.tsx, CnaeLabel.tsx
- Both index.tsx exports present
- QsaRelationshipCard.tsx imports and renders both new components
- QsaInlineSection.tsx imports and renders CnaeLabel
- `npm run build` completes without errors
</verification>

<success_criteria>
1. QsaRelationshipCard expanded section shows CSS-only segmented bar for score > 0, "Sem fatores de conflito" for score 0
2. Bar segments colored green (capital), amber (CNAE), blue (CPF) with active/inactive states
3. Legend with exact labels "Capital (50pts) / CNAE (30pts) / CPF (20pts)" visible below bar
4. CNAE labels show both description ("Construção de edifícios") and code ("41204") in each relationship
5. Conflict CNAE classes (41204, 70204, 73190, 86101) appear in amber text
6. CnaeLabel also renders in deputy profile QsaInlineSection relationship rows
</success_criteria>

<output>
Create `.planning/phases/06-polish-compliance/06-01-SUMMARY.md` when done
</output>
