---
phase: 06-polish-compliance
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - Frontend/components/fiscalizacao/DisclaimerBanner.tsx
  - Frontend/components/fiscalizacao/ConflictBadge.tsx
  - Frontend/components/fiscalizacao/index.tsx
  - Frontend/components/fiscalizacao/QsaRelationshipCard.tsx
  - Frontend/components/fiscalizacao/QsaInlineSection.tsx
  - Frontend/components/fiscalizacao/QsaSummaryCards.tsx
  - Frontend/components/fiscalizacao/QsaFilterBar.tsx
  - Frontend/components/fiscalizacao/QsaEmptyState.tsx
  - Frontend/app/fiscalizacao/FiscalizacaoDashboard.tsx
autonomous: true
requirements: [DOCS-03, QSA-14]
user_setup: []

must_haves:
  truths:
    - "User sees a score interpretation disclaimer banner on /fiscalizacao page"
    - "User sees an info icon on each score display that opens a tooltip with disclaimer text"
    - "All QSA pages render correctly on mobile viewports at 640px and 768px without overflow, cut-off content, or broken layouts"
    - "Card grid switches to single-column at sm breakpoint"
    - "Filter bar chips wrap correctly on narrow viewports"
    - "Pagination controls are tappable on mobile (adequate touch targets)"
  artifacts:
    - path: "Frontend/components/fiscalizacao/DisclaimerBanner.tsx"
      provides: "Static disclaimer banner component with exact text from D-12"
      min_lines: 15
    - path: "Frontend/components/fiscalizacao/FiscalizacaoDashboard.tsx"
      provides: "Disclaimer banner rendered below freshness banner, above summary cards"
      contains: "DisclaimerBanner"
    - path: "Frontend/components/fiscalizacao/ConflictBadge.tsx"
      provides: "Info icon with Tooltip wrapping disclaimer text on score display"
      contains: "Tooltip"
  key_links:
    - from: "FiscalizacaoDashboard.tsx"
      to: "DisclaimerBanner.tsx"
      via: "import and render below QsaFreshnessBanner"
      pattern: "DisclaimerBanner"
    - from: "ConflictBadge.tsx"
      to: "@/components/ui/tooltip"
      via: "Tooltip wrapping Info icon when score is defined"
      pattern: "Tooltip"
---

<objective>
**Deliverable:** Score interpretation disclaimer (DOCS-03) + Mobile responsive audit for all QSA pages (QSA-14).

**Purpose:** Legal/transparency compliance — users must understand scores are algorithmic indicators, not legal determinations. Mobile readiness ensures the transparency tool is accessible on phones and tablets, which is critical for a civic tech application.

**Output:** DisclaimerBanner component (page-level), Info icon with Tooltip on ConflictBadge (per-card), responsive fixes across all QSA pages at sm(640)/md(768) breakpoints.
</objective>

<execution_context>
@C:/Users/AndreC/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/AndreC/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-polish-compliance/06-CONTEXT.md
@.planning/phases/06-polish-compliance/06-RESEARCH.md
@.planning/phases/06-polish-compliance/06-01-SUMMARY.md (if exists — for new component interfaces)

<interfaces>
From Frontend/components/ui/tooltip.tsx:
```typescript
// shadcn/ui Tooltip — radix-ui wrapper, re-exported
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/tooltip";
```

From Frontend/components/fiscalizacao/ConflictBadge.tsx (current state):
```typescript
interface ConflictBadgeProps {
  hasConflict: boolean;
  score?: number;
}
// When score !== undefined, renders badge inside a <span className="group relative">
// When score is undefined, renders badge alone
// Target: add Info icon inside Tooltip next to score badge
```

From Frontend/app/fiscalizacao/FiscalizacaoDashboard.tsx (lines 107-168):
```typescript
// Structure:
// <div class="max-w-7xl ...">          ← main container
//   <h1>Fiscalização QSA</h1>
//   <QsaFreshnessBanner />             ← line ~114
//   {!loading && (
//     <>
//       <QsaSummaryCards />             ← line ~120
//       <QsaFilterBar />                ← line ~124
//       <QsaRelationshipCard[] />       ← line ~138
//       <pagination />                  ← line ~143
//     </>
//   )}
// </div>

// Target: Insert <DisclaimerBanner /> between <QsaFreshnessBanner /> and <QsaSummaryCards />
```

Current responsive patterns (from existing code):
- Card grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` — correct, needs verification
- Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` — correct
- Filter bar: horizontal flex row — may need wrapping at sm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DisclaimerBanner and add to FiscalizacaoDashboard page</name>
  <files>
    Frontend/components/fiscalizacao/DisclaimerBanner.tsx
    Frontend/app/fiscalizacao/FiscalizacaoDashboard.tsx
    Frontend/components/fiscalizacao/index.tsx
  </files>

  <read_first>
    Frontend/app/fiscalizacao/FiscalizacaoDashboard.tsx (lines 107-120 — layout between freshness banner and summary cards)
    Frontend/components/fiscalizacao/QsaFreshnessBanner.tsx (existing banner pattern for reference)
    .planning/phases/06-polish-compliance/06-CONTEXT.md (D-11, D-12)
  </read_first>

  <action>
    Create DisclaimerBanner.tsx:
    - Export `function DisclaimerBanner` — no props needed (static content)
    - Render: a blue-themed info banner with D-12 disclaimer text
    - Structure: flex row with Info icon (lucide-react) + text paragraph
    - Styling: `flex items-start gap-2 rounded-lg bg-blue-950/20 border border-blue-800/40 p-3`
    - Icon: `<Info className="w-4 h-4 text-blue-400 mt-0.5 shrink-0" />`
    - Text: `<p className="text-xs text-blue-300 leading-relaxed">` with the exact D-12 text
    - D-12 text: "Os scores são indicadores algorítmicos baseados em dados públicos da Receita Federal e não constituem determinação legal de conflito de interesses."
    - Per research: non-dismissable (static), no LocalStorage needed
    - Use `"use client"` directive per Next.js 16 app router convention

    Modify FiscalizacaoDashboard.tsx:
    - Import DisclaimerBanner at top
    - Insert `<DisclaimerBanner />` between the `<QsaFreshnessBanner />` and the `<QsaSummaryCards>` block
    - The insertion point is around line 117: after the closing `</QsaFreshnessBanner>` and before the `<QsaSummaryCards>` — inside the `{!loading &&` block
    - The disclaimer banner shows only when NOT loading (consistent with other content), inside the existing `{!loading && (` JSX fragment

    Update index.tsx:
    - Add export for DisclaimerBanner
  </action>

  <verify>
    <automated>
      Test-Path -LiteralPath "Frontend/components/fiscalizacao/DisclaimerBanner.tsx"
      Select-String -Path "Frontend/components/fiscalizacao/DisclaimerBanner.tsx" -Pattern "indicadores algorítmicos"
      Select-String -Path "Frontend/app/fiscalizacao/FiscalizacaoDashboard.tsx" -Pattern "DisclaimerBanner"
      Select-String -Path "Frontend/components/fiscalizacao/index.tsx" -Pattern "DisclaimerBanner"
      cd Frontend; if ($?) { npm run build --no-lint 2>&1 | tail -10 }
    </automated>
  </verify>

  <acceptance_criteria>
    - DisclaimerBanner.tsx exists with exported function DisclaimerBanner
    - Component contains exact D-12 disclaimer text string
    - Uses Info icon from lucide-react
    - Styled with bg-blue-950/20, border-blue-800/40, text-blue-300
    - FiscalizacaoDashboard.tsx imports and renders DisclaimerBanner
    - Banner inserted between QsaFreshnessBanner and QsaSummaryCards
    - index.tsx exports DisclaimerBanner
    - `npm run build` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add per-card disclaimer info icon to ConflictBadge with Tooltip</name>
  <files>
    Frontend/components/fiscalizacao/ConflictBadge.tsx
  </files>

  <read_first>
    Frontend/components/fiscalizacao/ConflictBadge.tsx (current state — full file, 34 lines)
    Frontend/components/ui/tooltip.tsx (radix-ui Tooltip component available as shadcn/ui)
    .planning/phases/06-polish-compliance/06-CONTEXT.md (D-11 "per-card info icon on each score display", D-12 disclaimer text)
  </read_first>

  <action>
    Add a disclaimer info icon with Tooltip to ConflictBadge when score is shown:
    - Add imports:
      ```
      import { Info } from "lucide-react";
      import {
        Tooltip,
        TooltipContent,
        TooltipTrigger,
      } from "@/components/ui/tooltip";
      import {
        TooltipProvider
      } from "@/components/ui/tooltip";
      ```

    - Locate the score display branch (line 25-31): `if (score !== undefined) { return (<span className={cn("group relative")}> {badge} </span>) }`
    - Replace this branch to include an info icon after the badge, wrapped in Tooltip:
      ```
      if (score !== undefined) {
        return (
          <span className={cn("inline-flex items-center")}>
            {badge}
            <TooltipProvider>
              <Tooltip>
                <TooltipTrigger asChild>
                  <button
                    onClick={(e) => e.stopPropagation()}
                    className="ml-1 inline-flex"
                    aria-label="Sobre os scores"
                  >
                    <Info className="w-3.5 h-3.5 text-gray-500 hover:text-gray-300" />
                  </button>
                </TooltipTrigger>
                <TooltipContent side="top" className="max-w-xs text-xs">
                  Os scores são indicadores algorítmicos baseados em dados públicos
                  da Receita Federal e não constituem determinação legal de conflito
                  de interesses.
                </TooltipContent>
              </Tooltip>
            </TooltipProvider>
          </span>
        );
      }
      ```

    - CRITICAL: Use `e.stopPropagation()` on the button's onClick to prevent the click from bubbling up to the parent card toggle handler (Research Pitfall 4). The QsaRelationshipCard has `onClick={handleToggle}` on the outer div.

    - Replace the className `"group relative"` with `"inline-flex items-center"` — the original class was likely intended for a planned tooltip that was never added. The new layout needs items-center for the badge + icon row alignment.

    - Keep the `hasConflict=false` branch (lines 9-16) unchanged — the info icon only appears when score is defined.

    - Do NOT wrap in a fragment or extra div — the existing <span> wrapper is sufficient.
  </action>

  <verify>
    <automated>
      Select-String -Path "Frontend/components/fiscalizacao/ConflictBadge.tsx" -Pattern "Tooltip.*from.*@/components/ui/tooltip"
      Select-String -Path "Frontend/components/fiscalizacao/ConflictBadge.tsx" -Pattern "Info.*from.*lucide-react"
      Select-String -Path "Frontend/components/fiscalizacao/ConflictBadge.tsx" -Pattern "stopPropagation"
      Select-String -Path "Frontend/components/fiscalizacao/ConflictBadge.tsx" -Pattern "indicadores algorítmicos"
      cd Frontend; if ($?) { npm run build --no-lint 2>&1 | tail -10 }
    </automated>
  </verify>

  <acceptance_criteria>
    - ConflictBadge.tsx imports Tooltip, TooltipContent, TooltipTrigger, TooltipProvider from @/components/ui/tooltip
    - ConflictBadge.tsx imports Info from lucide-react
    - Info icon rendered after badge when score is defined
    - Tooltip contains exact D-12 disclaimer text
    - stopPropagation() called on click to prevent card collapse trigger
    - hasConflict=false branch unchanged
    - `npm run build` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Mobile responsive audit and fix for all QSA pages at sm(640) and md(768) breakpoints</name>
  <files>
    Frontend/app/fiscalizacao/FiscalizacaoDashboard.tsx
    Frontend/components/fiscalizacao/QsaRelationshipCard.tsx
    Frontend/components/fiscalizacao/QsaInlineSection.tsx
    Frontend/components/fiscalizacao/QsaSummaryCards.tsx
    Frontend/components/fiscalizacao/QsaFilterBar.tsx
    Frontend/components/fiscalizacao/QsaEmptyState.tsx
  </files>

  <read_first>
    Frontend/app/fiscalizacao/FiscalizacaoDashboard.tsx (current page layout with new disclaimer banner)
    Frontend/components/fiscalizacao/QsaRelationshipCard.tsx (card with new ScoreBreakdownBar and CnaeLabel from 06-01)
    Frontend/components/fiscalizacao/QsaInlineSection.tsx (deputy profile section with new CnaeLabel from 06-01)
    Frontend/components/fiscalizacao/QsaSummaryCards.tsx (stats card row)
    Frontend/components/fiscalizacao/QsaFilterBar.tsx (filter/sort controls)
    Frontend/components/fiscalizacao/QsaEmptyState.tsx (empty state)
  </read_first>

  <action>
    Perform a full responsive audit on all QSA pages at sm(640) and md(768) breakpoints per D-09. Use only existing Tailwind sm/md/lg breakpoints per D-10. Check these states:

    1. **FiscalizacaoDashboard page layout** (FiscalizacaoDashboard.tsx):
       - Container already uses `px-4 sm:px-6 lg:px-8` and `max-w-7xl` — verify no overflow
       - Heading `text-2xl` — adequate at sm
       - DisclaimerBanner — verify the banner text wraps correctly (the `text-xs` + `leading-relaxed` should handle this; add `text-xs sm:text-sm` if it's too cramped at 640px)

    2. **Card grid** (FiscalizacaoDashboard.tsx line 136):
       - Already `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` — correct responsive grid
       - Verify the gap is adequate at md(768): `gap-4` should be fine
       - No change needed unless testing reveals issues

    3. **QsaRelationshipCard expanded section** (QsaRelationshipCard.tsx):
       - ScoreBreakdownBar legend with three items — may wrap at sm(640). Ensure `flex-wrap` or `gap-3` handles wrapping gracefully
       - Badge row with ConflictBadge, MatchTypeBadge, ExposureIndicator, SpouseDisclosure — already uses `flex-wrap gap-2`. Verify all badges fit
       - CnaeLabel: description + code in flex row — verify no truncation at narrow widths
       - If the badge row is too crowded, change `gap-2` to `gap-1.5` and reduce font to `text-[11px]` on sm

    4. **QsaInlineSection relationship rows** (QsaInlineSection.tsx):
       - Each row stacks company name, CNPJ, capital, CnaeLabel, badge row
       - Badge row already `flex-wrap gap-2` — verify at sm
       - CnaeLabel description may be long — ensure it wraps within container width

    5. **QsaSummaryCards** (QsaSummaryCards.tsx):
       - Check if it uses a grid; ensure it switches to single column or adequate wrapping at sm
       - If it uses `grid-cols-4` or similar, add `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`

    6. **QsaFilterBar** (QsaFilterBar.tsx):
       - Filter chips and sort dropdown — ensure wrapping or scrolling at sm
       - Add `flex-wrap` if not present; sort dropdown should remain full-width or inline as appropriate
       - At sm(640), consider stacking filter row above sort dropdown instead of inline

    7. **QsaEmptyState** (QsaEmptyState.tsx):
       - Verify icon and text don't overflow; padding should be responsive
       - If padding is fixed, change to `p-8 sm:p-12` pattern

    8. **Pagination controls** (FiscalizacaoDashboard.tsx lines 142-162):
       - Buttons should have adequate touch targets at sm (min 44px tap target)
       - Currently `px-4 py-2` — verify at sm. If too small, add `sm:px-4 sm:py-2` (already default) or ensure py is adequate
       - The "Anterior / page number / Próxima" layout should stack or scroll at sm if needed

    9. **Dynamic states per research Pitfall 4**:
       - Expand a card at sm(640) — verify the expanded content doesn't overflow the viewport
       - Open sort dropdown at sm(640) — verify dropdown is positioned correctly
       - Apply multiple filters at sm(640) — verify active filter chips don't overflow
       - Pagination at large page numbers at md(768) — verify text doesn't cause overflow

    **Implementation approach:**
    - For each issue found, apply Tailwind responsive prefix fixes
    - Priority: fix overflow issues first, then spacing, then visual polish
    - Test implicitly by ensuring no horizontal scrollbar at 640px and 768px viewport widths
    - Use `overflow-x-auto` as LAST resort — prefer `flex-wrap`, `grid-cols-1`, and responsive padding

    **D-09 requires checking these specific viewports:**
    - sm(640px): single-column grid, wrapped filter chips, stacked card layout
    - md(768px): 2-column grid for cards, filter bar should fit inline

    **Per discretion:** Use standard Tailwind responsive patterns. Document any judgment calls in the task summary.
  </action>

  <verify>
    <automated>
      cd Frontend; if ($?) { npm run build --no-lint 2>&1 | tail -10 }
    </automated>
    <human-check>
      Cannot fully automate responsive visual check. After automated fixes:
      1. Open Frontend dev server: `npm run dev` in Frontend/
      2. Open browser to http://localhost:3000/fiscalizacao
      3. Open DevTools responsive mode
      4. Test at 640px width: card grid shows 1 column, filter chips wrap, no horizontal scroll
      5. Test at 768px width: card grid shows 2 columns, filter bar fits, expanded card content doesn't overflow
      6. Test at 375px (phone): all content visible, no overflow, touch targets adequate
      7. Test expanded card states at each breakpoint
      8. Navigate to a deputy profile page, expand QSA section at each breakpoint
    </human-check>
  </verify>

  <acceptance_criteria>
    - At sm(640px): card grid is single column, filter chips wrap without overflow, all badge rows wrap, scrolled/expanded content fits viewport width, no horizontal scrollbar
    - At md(768px): card grid is 2 columns, filter bar controls fit inline, pagination buttons don't overlap
    - ScoreBreakdownBar legend wraps gracefully at narrow widths
    - CnaeLabel description text doesn't overflow or truncate awkwardly
    - All interactive elements (filter chips, sort dropdown, pagination buttons, expand toggles, info icons) have adequate touch targets at sm
    - No custom breakpoints introduced — only sm/md/lg per D-10
    - `npm run build` succeeds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→DOM | Disclaimer banner renders static text — no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | DisclaimerBanner | accept | Static component with hardcoded text — no user-modifiable content. |
</threat_model>

<verification>
- DisclaimerBanner.tsx exists with exact D-12 text
- FiscalizacaoDashboard.tsx renders DisclaimerBanner below freshness banner
- ConflictBadge.tsx shows Info icon with Tooltip when score is defined
- stopPropagation() present on info icon button
- All QSA pages render without horizontal overflow at 640px and 768px
- `npm run build` succeeds
</verification>

<success_criteria>
1. Disclaimer banner visible on /fiscalizacao page with blue styling and exact Portuguese disclaimer text
2. Info icon appears on every ConflictBadge that shows a score (both card expanded section and deputy profile)
3. Clicking info icon does NOT toggle card expand state
4. Tooltip shows disclaimer text on hover/focus
5. QSA pages fully responsive at sm(640) and md(768) breakpoints
6. Card grid adapts: 1 col → 2 col → 3 col across sm/md/lg
7. Filter bar, summary cards, pagination, and badge rows all wrap correctly without overflow
</success_criteria>

<output>
Create `.planning/phases/06-polish-compliance/06-02-SUMMARY.md` when done
</output>
