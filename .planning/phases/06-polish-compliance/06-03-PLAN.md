---
phase: 06-polish-compliance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation/01-VALIDATION.md
  - .planning/phases/02-enhancement/02-VALIDATION.md
  - .planning/phases/03-intelligence/03-VALIDATION.md
  - docker-compose.yml
autonomous: false
requirements: [DOCS-01, DOCS-02, DOCS-04]
user_setup:
  - service: docker
    why: "End-to-end build verification (docker-compose up --build)"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Citizen can access VALIDATION.md for all 3 v1.0 phases with requirements traceability"
    - "Docker compose starts both services with healthchecks that auto-detect unhealthy containers"
    - "Frontend waits for backend to be healthy before starting (depends_on condition: service_healthy)"
    - "QSA data persists across container restart via named volume"
    - "Nav link to /fiscalizacao exists in shared navigation"
  artifacts:
    - path: ".planning/phases/01-foundation/01-VALIDATION.md"
      provides: "Requirements traceability matrix for Phase 1 with 12 requirements"
      min_lines: 30
    - path: ".planning/phases/02-enhancement/02-VALIDATION.md"
      provides: "Requirements traceability matrix for Phase 2 with 9 requirements"
      min_lines: 30
    - path: ".planning/phases/03-intelligence/03-VALIDATION.md"
      provides: "Requirements traceability matrix for Phase 3 with 2 requirements"
      min_lines: 20
    - path: "docker-compose.yml"
      provides: "Healthcheck configuration for both backend and frontend services"
      contains: "healthcheck"
  key_links:
    - from: "docker-compose.yml"
      to: "Backend/app/main.py"
      via: "python -c urllib healthcheck hits GET / endpoint"
      pattern: "urllib.*localhost.*3001"
    - from: "docker-compose.yml frontend depends_on"
      to: "docker-compose.yml backend healthcheck"
      via: "condition: service_healthy"
      pattern: "service_healthy"
---

<objective>
**Deliverable:** VALIDATION.md for all 3 v1.0 phases (DOCS-01) + Docker healthcheck wiring with end-to-end build test (DOCS-04) + DOCS-02 verification (nav link already exists).

**Purpose:** Nyquist compliance requires VALIDATION.md — "was the system validated from a user perspective?" Docker healthchecks ensure production-grade container orchestration (auto-recovery of unhealthy services). DOCS-02 verification confirms Phase 5's nav link work is complete.

**Output:** Three VALIDATION.md files (requirements traceability matrices), docker-compose.yml with healthchecks on both services, build verification.
</objective>

<execution_context>
@C:/Users/AndreC/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/AndreC/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-compliance/06-CONTEXT.md
@.planning/phases/06-polish-compliance/06-RESEARCH.md

<interfaces>
From docker-compose.yml (current state, 31 lines):
```yaml
version: "3.9"
services:
  backend:
    build: { context: ./Backend, dockerfile: Dockerfile }
    ports: ["3001:3001"]
    environment:
      - DATABASE_URL=sqlite:///data/db.sqlite3
      - PORT=3001
      - CAMARA_API_BASE_URL=https://dadosabertos.camara.leg.br/api/v2
    volumes: [backend_data:/app/data]
    restart: unless-stopped

  frontend:
    build: { context: ./Frontend, dockerfile: Dockerfile }
    ports: ["3000:3000"]
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:3001
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  backend_data:
```

Backend healthcheck endpoint (from Backend/app/main.py:50-52):
```python
@app.get("/")
def health_check():
    return {"status": "ok", "message": "Backend Python (FastAPI) Rodando"}
```

DOCS-02 verification (from Frontend/components/ui/NavbarWithSearch.tsx:82-86):
```tsx
<Link href="/fiscalizacao" className="px-3 py-1.5 rounded-lg hover:text-white hover:bg-gray-800 transition-all">
  Fiscalização
</Link>
```

VALIDATION.md format (from 01-VERIFICATION.md):
- Frontmatter: phase, phase_name, milestone, validation_date, status
- Requirements Summary table
- Requirement-by-Requirement Mapping table with columns: REQ-ID, Description, Status, Validation Evidence, Automated Test, Validated
- Validation Evidence section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VALIDATION.md for phases 1, 2, and 3</name>
  <files>
    .planning/phases/01-foundation/01-VALIDATION.md
    .planning/phases/02-enhancement/02-VALIDATION.md
    .planning/phases/03-intelligence/03-VALIDATION.md
  </files>

  <read_first>
    .planning/phases/01-foundation/01-VERIFICATION.md (existing verification file for Phase 1 — source of requirement evidence)
    .planning/phases/02-enhancement/02-VERIFICATION.md (existing verification file for Phase 2)
    .planning/phases/03-intelligence/03-VERIFICATION.md (existing verification file for Phase 3)
    .planning/phases/06-polish-compliance/06-CONTEXT.md (D-13 "one file per phase", D-14 "requirements traceability matrix format")
    .planning/phases/06-polish-compliance/06-RESEARCH.md (VALIDATION.md structure pattern at lines 427-450, plus Pitfall 3: distinguish from VERIFICATION.md)
  </read_first>

  <action>
    Create three VALIDATION.md files following D-13 and D-14:

    **Format for each VALIDATION.md (per D-14 — requirements traceability matrix):**

    ```markdown
    ---
    phase: 0X-name
    phase_name: [Foundation | Enhancement | Intelligence]
    milestone: v1.0
    validation_date: "2026-06-15"
    status: VALIDATED
    ---

    # Phase X: [Name] — Validation

    ## Validation Scope

    [Brief paragraph describing what the phase delivered from a user/citizen perspective.]

    ## Requirements Traceability Matrix

    | REQ-ID | Description | Status | Validation Evidence | Automated Test | Validated |
    |--------|-------------|--------|---------------------|----------------|-----------|
    | [ID] | [short desc] | ✅ | [specific reference] | [test file/name] | ✅ |

    ## Validation Evidence

    [For each requirement, describe the validation outcome — what acceptance criteria were met from the user's perspective. This is different from VERIFICATION.md which describes implementation correctness.]

    ## Known Limitations

    [Any requirements with ⚠️ or ❌ status, or accepted limitations documented here.]

    ## Validation Conclusion

    [Summary of validation result — how many requirements validated, any caveats.]
    ```

    **Per D-13 create these files:**

    **01-VALIDATION.md** (for Phase 1 Foundation):
    - Validation from USER perspective: "A citizen can trigger QSA data ingestion, the system processes official Receita Federal data, deputy-company relationships are established via CPF matching, and CNPJ validation ensures data integrity."
    - Requirements to map: ING-01, ING-02, ING-03, MAT-01, MAT-03, MAT-04, API-01, API-03, INF-01, INF-03, DQ-02, DQ-03
    - For INF-01 (Docker): mark as ❌ with note "Deferred to v1.1 Phase 6 DOCS-04 — healthchecks added in Phase 6 resolve this gap"
    - Reference evidence from 01-VERIFICATION.md requirement table (the "Evidence Source" and "UAT Passed" columns)
    - UAT evidence sources: Phase 1 UAT results from the milestone audit

    **02-VALIDATION.md** (for Phase 2 Enhancement):
    - Validation from USER perspective: "The system uses fuzzy name matching to catch spouse-linked companies, flags high financial exposure and spouse relationships, provides paginated filtered listing, tracks data freshness, and optimizes query performance with database indices."
    - Requirements: MAT-02, CONF-03, CONF-04, API-02, API-04, INF-02, INF-04, DQ-01, DQ-04
    - API-02: mark as ⚠️ Partial with note "No 02-04-SUMMARY.md filed, but endpoint works correctly with all filters — validated via UAT test 3"
    - Reference 02-VERIFICATION.md for evidence

    **03-VALIDATION.md** (for Phase 3 Intelligence):
    - Validation from USER perspective: "Each deputy-company relationship receives a graduated conflict score based on capital exposure (50pts), CNAE class (30pts), and CPF match (20pts). CNAE conflict flags indicate potential conflicts of interest in high-risk sectors."
    - Requirements: CONF-01, CONF-02
    - Both ✅ — reference 03-VERIFICATION.md evidence and 03-UAT.md test results

    **Research Pitfall 3 — Distinguish VALIDATION from VERIFICATION:**
    - VALIDATION.md answers: "Does the delivered system meet the requirements from a user perspective?"
    - VERIFICATION.md answers: "Was the implementation correct?"
    - Use language like "A user can..." / "The system behaves..." / "Acceptance criteria met..." rather than "Implementation verified..."
    - Include the requirements traceability matrix per D-14
  </action>

  <verify>
    <automated>
      # Verify all 3 VALIDATION.md files exist
      Test-Path -LiteralPath ".planning/phases/01-foundation/01-VALIDATION.md"
      Test-Path -LiteralPath ".planning/phases/02-enhancement/02-VALIDATION.md"
      Test-Path -LiteralPath ".planning/phases/03-intelligence/03-VALIDATION.md"

      # Verify frontmatter and traceability matrix structure
      Select-String -Path ".planning/phases/01-foundation/01-VALIDATION.md" -Pattern "status: VALIDATED"
      Select-String -Path ".planning/phases/01-foundation/01-VALIDATION.md" -Pattern "Requirements Traceability Matrix"
      
      Select-String -Path ".planning/phases/02-enhancement/02-VALIDATION.md" -Pattern "status: VALIDATED"
      Select-String -Path ".planning/phases/02-enhancement/02-VALIDATION.md" -Pattern "Requirements Traceability Matrix"
      
      Select-String -Path ".planning/phases/03-intelligence/03-VALIDATION.md" -Pattern "status: VALIDATED"
      Select-String -Path ".planning/phases/03-intelligence/03-VALIDATION.md" -Pattern "Requirements Traceability Matrix"

      # Verify each file has all its requirements listed
      # Phase 1 has 12 requirements (ING-01, ING-02, ING-03, MAT-01, MAT-03, MAT-04, API-01, API-03, INF-01, INF-03, DQ-02, DQ-03)
      $p1 = Select-String -Path ".planning/phases/01-foundation/01-VALIDATION.md" -Pattern "^\\| [A-Z]+-" | Measure-Object | Select-Object -ExpandProperty Count
      if ($p1 -lt 12) { Write-Error "Phase 1 VALIDATION.md missing requirements" } else { Write-Output "Phase 1: $p1 requirements found" }
      
      # Phase 2 has 9 requirements
      $p2 = Select-String -Path ".planning/phases/02-enhancement/02-VALIDATION.md" -Pattern "^\\| [A-Z]+-" | Measure-Object | Select-Object -ExpandProperty Count
      if ($p2 -lt 9) { Write-Error "Phase 2 VALIDATION.md missing requirements" } else { Write-Output "Phase 2: $p2 requirements found" }
      
      # Phase 3 has 2 requirements
      $p3 = Select-String -Path ".planning/phases/03-intelligence/03-VALIDATION.md" -Pattern "^\\| [A-Z]+-" | Measure-Object | Select-Object -ExpandProperty Count
      if ($p3 -lt 2) { Write-Error "Phase 3 VALIDATION.md missing requirements" } else { Write-Output "Phase 3: $p3 requirements found" }
    </automated>
  </verify>

  <acceptance_criteria>
    - 01-VALIDATION.md exists with frontmatter status: VALIDATED
    - 02-VALIDATION.md exists with frontmatter status: VALIDATED
    - 03-VALIDATION.md exists with frontmatter status: VALIDATED
    - Each file contains a "Requirements Traceability Matrix" section
    - Phase 1 matrix covers all 12 requirements
    - Phase 2 matrix covers all 9 requirements
    - Phase 3 matrix covers both CONF-01 and CONF-02
    - INF-01 marked as ❌ (deferred, resolved by Phase 6 DOCS-04)
    - API-02 marked as ⚠️ Partial (no 02-04-SUMMARY.md)
    - Validation evidence references user-facing acceptance criteria, not implementation verification
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Docker healthchecks to docker-compose.yml</name>
  <files>
    docker-compose.yml
  </files>

  <read_first>
    docker-compose.yml (current state — 31 lines, both backend and frontend services)
    Backend/app/main.py (lines 50-52 — GET / returns {"status": "ok"})
    .planning/phases/06-polish-compliance/06-CONTEXT.md (D-15 "healthcheck config to both services", D-16 "run docker-compose up --build", D-17 "verify persistence")
    .planning/phases/06-polish-compliance/06-RESEARCH.md (Docker Healthcheck Pattern at lines 399-424, Pitfall 3 about missing curl, A1 about wget)
  </read_first>

  <action>
    Modify docker-compose.yml to add healthchecks and proper dependency conditions:

    **Backend service** — add healthcheck block:
    ```yaml
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:3001/')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    ```
    - Uses `python -c urllib` per Research Pitfall 3 — `python:3.13-slim` does NOT include curl
    - Backend's `GET /` already returns `{"status": "ok"}` (Backend/app/main.py:50-52)
    - Port 3001 is the backend's internal port per existing docker-compose config

    **Frontend service** — add healthcheck block AND update depends_on:
    ```yaml
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    ```
    - Uses `wget --spider` — Alpine image includes wget in BusyBox by default per Research A1
    - `depends_on` update from `- backend` (bare) to `backend: condition: service_healthy` — ensures frontend waits for backend to be healthy before starting
    - Port 3000 is the frontend's internal port per existing config

    **Keep all existing config unchanged:**
    - `version: "3.9"`, `volumes:`, `restart:`, `ports:`, `environment:`, `build:` all stay as-is
    - Do NOT change port mappings or env vars

    **Per D-17:** Volume persistence will be verified in Task 3's build test. The `backend_data` volume is already configured — no changes needed.
  </action>

  <verify>
    <automated>
      # Verify healthcheck blocks exist
      Select-String -Path "docker-compose.yml" -Pattern "healthcheck:"
      $hcCount = Select-String -Path "docker-compose.yml" -Pattern "healthcheck:" | Measure-Object | Select-Object -ExpandProperty Count
      if ($hcCount -lt 2) { Write-Error "Expected 2 healthcheck blocks, found $hcCount" }
      
      Select-String -Path "docker-compose.yml" -Pattern "service_healthy"
      Select-String -Path "docker-compose.yml" -Pattern "urllib\\.request\\.urlopen"
      Select-String -Path "docker-compose.yml" -Pattern "wget.*--spider"
      
      # Verify existing config preserved
      Select-String -Path "docker-compose.yml" -Pattern "backend_data:"
      Select-String -Path "docker-compose.yml" -Pattern "NEXT_PUBLIC_API_URL"
      Select-String -Path "docker-compose.yml" -Pattern "PORT=3001"
    </automated>
  </verify>

  <acceptance_criteria>
    - docker-compose.yml contains healthcheck block for backend service with `python -c "import urllib.request; urllib.request.urlopen('http://localhost:3001/')"`
    - docker-compose.yml contains healthcheck block for frontend service with `wget --spider http://localhost:3000/`
    - Frontend `depends_on` uses `condition: service_healthy` instead of bare `- backend`
    - All existing environment variables, ports, volumes, build contexts, and restart policies preserved
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking" tdd="false">
  <name>Task 3: Docker build test, persistence verification, and DOCS-02 verification</name>
  <files>
    docker-compose.yml
    .planning/phases/06-polish-compliance/06-03-SUMMARY.md (where DOCS-02 verification note will be recorded)
  </files>

  <read_first>
    docker-compose.yml (now with healthchecks from Task 2)
    Frontend/components/ui/NavbarWithSearch.tsx (lines 82-86 — /fiscalizacao nav link)
  </read_first>

  <action>
    Perform three verifications:

    **A. DOCS-02 nav link verification (5 min):**
    - Examine Frontend/components/ui/NavbarWithSearch.tsx lines 82-86
    - Confirm: `<Link href="/fiscalizacao"` exists with "Fiscalização" label
    - Record in 06-03-SUMMARY.md: "DOCS-02: ✅ Nav link to /fiscalizacao verified present at NavbarWithSearch.tsx:82-86. Fulfilled by Phase 5."

    **B. Docker build and healthcheck test:**
    - Run: `docker-compose up --build -d`
    - Wait for services to start (at least 30s for healthcheck start_period)
    - Verify both services report healthy: `docker ps --filter "health=healthy"` should list both services
    - Test backend health: `curl http://localhost:3001/` or PowerShell: `Invoke-RestMethod -Uri "http://localhost:3001/"`
    - Test frontend health: `curl http://localhost:3000/` or `Invoke-WebRequest -Uri "http://localhost:3000/" -UseBasicParsing`
    - Stop: `docker-compose down`

    **C. QSA data persistence (D-17):**
    - Start: `docker-compose up -d`
    - Trigger QSA sync: `Invoke-RestMethod -Method Post -Uri "http://localhost:3001/api/integracao/sync"`
    - Verify data returns: `Invoke-RestMethod -Uri "http://localhost:3001/api/v1/freshness"`
    - Stop: `docker-compose down`
    - Start again: `docker-compose up -d`
    - Verify data persists (same freshness endpoint returns data, proving volume mapping works):
      `Invoke-RestMethod -Uri "http://localhost:3001/api/v1/freshness"`
    - If data persists, record: "DOCS-04: ✅ Docker build succeeds, healthchecks pass, QSA data persists across restarts via backend_data volume."
    - Stop: `docker-compose down`

    If docker-compose is not available, this task cannot complete. Signal the issue in the verification output.

    **Note to executor:** This task requires Docker Desktop running on Windows. If it's not installed or Docker daemon is not running, report the issue and skip to documentation-only verification (DOCS-02 nav link check).
  </action>

  <verify>
    <human-check>
      After automated Docker build and healthcheck verification:
      1. Run `docker-compose up --build -d` from project root
      2. Wait ~30s, run `docker ps` to check both services are "healthy"
      3. Verify `Invoke-RestMethod http://localhost:3001/` returns `{"status": "ok"}`
      4. Verify `Invoke-RestMethod http://localhost:3000/` returns Next.js HTML (200 OK)
      5. Run QSA sync, stop containers, start again, verify data persists
      6. Run `docker-compose down` to clean up
    </human-check>
  </verify>

  <what-built>
    Docker infrastructure verification and DOCS-02 completion check.
  </what-built>

  <how-to-verify>
    1. From project root, run `docker-compose up --build -d`
    2. Wait 30 seconds
    3. Run `docker ps` — both containers should show "(healthy)" status
    4. Test backend: `curl http://localhost:3001/` → expect `{"status":"ok"}`
    5. Test frontend: open http://localhost:3000 in browser — page loads
    6. For persistence: POST `/api/integracao/sync`, then docker-compose down, then up again, data should still be there
    7. Check NavbarWithSearch.tsx:82-86 for `/fiscalizacao` link
    8. Run `docker-compose down` to clean up
  </how-to-verify>

  <resume-signal>Docker build and healthchecks confirmed working, and DOCS-02 nav link verified.</resume-signal>

  <acceptance_criteria>
    - DOCS-02: /fiscalizacao nav link confirmed present in NavbarWithSearch.tsx:82-86
    - DOCS-04: docker-compose up --build succeeds with both services starting
    - Both services report healthy via healthcheck (docker ps shows "(healthy)")
    - Backend health endpoint (GET /) returns {"status": "ok"}
    - Frontend serves HTML on port 3000
    - QSA data persists across container restart (stop + start retains data)
    - Verification findings recorded in 06-03-SUMMARY.md
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container→localhost | Docker healthchecks make HTTP requests to localhost only |
| host→container volume | QSA SQLite data persisted via named volume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | docker-compose.yml healthcheck | mitigate | Healthcheck uses Python stdlib `urllib` and Alpine `wget` — no `curl` installed, reducing attack surface. Both checks target localhost only (no external network). |
| T-06-03-02 | Denial of Service | Healthcheck interval | accept | 30s interval with 3 retries prevents healthcheck storm. start_period 15s gives container initialization time. |
</threat_model>

<verification>
- 01-VALIDATION.md, 02-VALIDATION.md, 03-VALIDATION.md all exist with requirements traceability matrices
- docker-compose.yml has healthcheck blocks on both services
- NavbarWithSearch.tsx:82-86 confirmed to have /fiscalizacao link
- docker-compose up --build succeeds
- Both services report healthy
- QSA data persists across container restart
</verification>

<success_criteria>
1. VALIDATION.md exists for all 3 v1.0 phases with requirements traceability matrix format
2. Each VALIDATION.md distinguishes validation outcomes (user acceptance) from verification (implementation correctness)
3. docker-compose.yml has healthchecks on backend (python -c urllib) and frontend (wget --spider)
4. Frontend depends_on: backend condition: service_healthy
5. docker-compose up --build produces healthy containers
6. QSA data survives container restart via backend_data volume
7. DOCS-02 confirmed complete with file+line reference
</success_criteria>

<output>
Create `.planning/phases/06-polish-compliance/06-03-SUMMARY.md` when done
</output>
