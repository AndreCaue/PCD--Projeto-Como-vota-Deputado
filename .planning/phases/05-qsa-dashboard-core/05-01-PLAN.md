---
phase: 05-qsa-dashboard-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Backend/app/api/deputados.py
  - Frontend/services/api.ts
autonomous: true
requirements: [QSA-01, QSA-08, QSA-09]
user_setup: []

must_haves:
  truths:
    - "User can sort the deputy list by score, capital, or name"
    - "User can filter by spouse-linked relationships"
    - "Summary cards show total_alta_exposicao and total_conjuge counts"
    - "Frontend types match backend response shape"
  artifacts:
    - path: "Backend/app/api/deputados.py"
      provides: "Enhanced GET /deputados/empresas with sort_by, sort_order, tem_conjuge, total_alta_exposicao, total_conjuge"
      contains: "total_alta_exposicao"
    - path: "Frontend/services/api.ts"
      provides: "Updated DeputadoEmpresa interface with total_alta_exposicao and total_conjuge fields"
      contains: "total_alta_exposicao"
  key_links:
    - from: "Frontend/services/api.ts"
      to: "Backend/app/api/deputados.py"
      via: "GET /deputados/empresas response shape"
      pattern: "total_alta_exposicao"
---

<objective>
Enhance the backend `/deputados/empresas` endpoint with sort parameters, spouse filter, and additional aggregate fields — then mirror the response shape in the frontend type definitions.

**Purpose:** Unblock the QSA dashboard pagination, filter-by-spouse, sort-dropdown, and aggregate summary card features. Without these changes, D-09 (Spouse chip), D-10 (Sort dropdown), and D-03 (exposure/conjuge summary cards) cannot function correctly with server-side pagination.

**Output:**
1. `GET /deputados/empresas` accepts `sort_by`, `sort_order`, `tem_conjuge` query params
2. Response includes `total_alta_exposicao` and `total_conjuge` per deputy
3. `DeputadoEmpresa` interface in `api.ts` includes both new fields
</objective>

<execution_context>
@C:/Users/AndreC/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/AndreC/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 5 goal and success criteria
@.planning/phases/05-qsa-dashboard-core/05-CONTEXT.md — Locked decisions D-03, D-09, D-10
@.planning/phases/05-qsa-dashboard-core/05-RESEARCH.md — Backend gaps analysis (Pitfalls 1-2), data shapes, sort/spouse limitation details

<interfaces>
From Backend/app/api/deputados.py (lines 13-101):
```python
# Current endpoint signature — add sort_by, sort_order, tem_conjuge params
@router.get("/empresas")
def list_deputados_empresas(
    partido: str = None,
    estado: str = None,
    tem_conflito: bool = None,
    alta_exposicao: bool = None,
    conflito_interesse: bool = None,
    page: int = Query(1, ge=1, le=200),
    limit: int = Query(50, ge=1, le=200),
    db: Session = Depends(get_db)
):

# Current aggregate query (lines 26-36) — add total_alta_exposicao, total_conjuge aggregations
query = db.query(
    Deputado.id, Deputado.nome, Partido.sigla.label("partido"), Deputado.estado,
    func.count(Relacao.cnpj).label("total_empresas"),
    func.sum(case((Relacao.conflito_interesse == True, 1), else_=0)).label("total_conflito"),
)
```

From Frontend/services/api.ts (lines 229-236):
```typescript
export interface DeputadoEmpresa {
  id: string;
  nome: string;
  partido: string;
  estado: string;
  total_empresas: number;
  total_conflito: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sort params + aggregate counts to GET /deputados/empresas</name>
  <read_first>
    Backend/app/api/deputados.py — Current endpoint implementation (lines 24-101)
    .planning/phases/05-qsa-dashboard-core/05-RESEARCH.md — Pitfall 1 analysis, data shapes, query structure notes
  </read_first>
  <files>
    Backend/app/api/deputados.py
  </files>
  <action>
    Add three capabilities to the `list_deputados_empresas` endpoint:

    1. **Sort params**: Add `sort_by: str = Query(None)` and `sort_order: str = Query("desc")` function params after `limit`. Accept `sort_by` values: `"score"` (maps to total_conflito), `"capital"` (maps to func.sum(Relacao.capital_social)), `"nome"` (maps to Deputado.nome). Accept `sort_order` values: `"asc"`, `"desc"`. When `sort_by` is provided, replace line 60's `order_by(func.count(Relacao.cnpj).desc())` with a conditional order_by using the mapped column expression and direction. When `sort_by` is None, keep the current default ordering.

    2. **Aggregate counts**: Add two new columns to the query select list (lines 26-36):
       - `func.sum(case((Relacao.alta_exposicao == True, 1), else_=0)).label("total_alta_exposicao")`
       - `func.sum(case((Relacao.via_conjuge == True, 1), else_=0)).label("total_conjuge")`
       Include `"total_alta_exposicao"` and `"total_conjuge"` in the response dict (lines 83-92), reading from `r.total_alta_exposicao` and `r.total_conjuge`.

    3. **Spouse filter**: Add `tem_conjuge: bool = None` function param. Implement filter logic matching the existing `tem_conflito` pattern (lines 45-54): when `tem_conjuge is True`, filter `Relacao.via_conjuge == True`; when `tem_conjuge is False`, exclude deputies who have any `Relacao.via_conjuge == True`. Position `tem_conjuge` check after `alta_exposicao` (line 58) and before the `group_by` call.
  </action>
  <acceptance_criteria>
    1. Backend starts without errors (uvicorn on port 3001)
    2. `curl "http://localhost:3001/deputados/empresas?sort_by=nome&sort_order=asc"` returns deputies sorted alphabetically by name ascending
    3. `curl "http://localhost:3001/deputados/empresas?sort_by=score&sort_order=desc"` returns deputies sorted by total_conflito descending
    4. `curl "http://localhost:3001/deputados/empresas?sort_by=capital&sort_order=desc"` returns deputies sorted by sum of capital_social descending (order may vary based on capital data)
    5. `curl "http://localhost:3001/deputados/empresas?tem_conjuge=true"` returns only deputies with at least one via_conjuge relationship
    6. Response JSON includes `total_alta_exposicao` and `total_conjuge` integer fields in every data item
    7. Default call `curl "http://localhost:3001/deputados/empresas"` preserves original behavior
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update DeputadoEmpresa interface in Frontend</name>
  <read_first>
    Frontend/services/api.ts — Current DeputadoEmpresa interface (lines 229-236)
    Frontend/tsconfig.json — Verify path aliases
  </read_first>
  <files>
    Frontend/services/api.ts
  </files>
  <action>
    Add two optional number fields to the `DeputadoEmpresa` interface: `total_alta_exposicao?: number` and `total_conjuge?: number`. They must be marked optional (`?:`) so existing code compiles during deployment transitions where old API responses lack these fields. Keep all existing fields unchanged.
  </action>
  <acceptance_criteria>
    1. `Frontend/services/api.ts` compiles with `npx tsc --noEmit` (or `npm run build`) without errors
    2. `DeputadoEmpresa` interface has `total_alta_exposicao?: number` and `total_conjuge?: number` fields
    3. All existing fields (id, nome, partido, estado, total_empresas, total_conflito) remain unchanged
    4. Existing consumers of DeputadoEmpresa still compile without modification
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Public user-agent sends query params to backend endpoint; no auth, no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `list_deputados_empresas` sort params | mitigate | Validate `sort_by` against whitelist (`score`, `capital`, `nome`); reject unknown values with 422 |
| T-05-02 | Tampering | `tem_conjuge` filter param | mitigate | FastAPI `bool` type coerces safely; no SQL injection risk via query param (SQLAlchemy parameterized queries) |
| T-05-SC | Tampering | pip install slop packages | accept | No new pip packages — existing dependencies only |
</threat_model>

<verification>
1. `cd Backend && .\venv\Scripts\activate && uvicorn app.main:app --port 3001` starts without errors
2. `curl "localhost:3001/deputados/empresas?sort_by=nome&sort_order=asc" | python -m json.tool` returns valid JSON with sorted results
3. `curl "localhost:3001/deputados/empresas?tem_conjuge=true" | python -m json.tool` returns filtered results
4. Response items contain `total_alta_exposicao` and `total_conjuge` integer fields
5. `cd Frontend && npx tsc --noEmit` passes with updated DeputadoEmpresa interface
</verification>

<success_criteria>
1. Backend endpoint supports sort_by, sort_order, tem_conjuge query params
2. Backend response includes total_alta_exposicao and total_conjuge aggregate fields
3. Frontend type definitions match the enhanced response shape
4. Default behavior (no params) is unchanged from original
</success_criteria>
