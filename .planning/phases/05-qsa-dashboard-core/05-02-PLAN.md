---
phase: 05-qsa-dashboard-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Frontend/components/ui/tooltip.tsx
  - Frontend/components/fiscalizacao/ConflictBadge.tsx
  - Frontend/components/fiscalizacao/MatchTypeBadge.tsx
  - Frontend/components/fiscalizacao/ExposureIndicator.tsx
  - Frontend/components/fiscalizacao/SpouseDisclosure.tsx
  - Frontend/components/fiscalizacao/QsaEmptyState.tsx
  - Frontend/components/fiscalizacao/QsaErrorState.tsx
  - Frontend/components/fiscalizacao/QsaFreshnessBanner.tsx
  - Frontend/components/fiscalizacao/index.tsx
autonomous: true
requirements: [QSA-03, QSA-04, QSA-05, QSA-06, QSA-07, QSA-11]
user_setup: []

must_haves:
  truths:
    - "ConflictBadge shows red/green dot + label + tooltip with score"
    - "MatchTypeBadge shows green 'CPF' or amber 'Nome' with tooltip"
    - "ExposureIndicator shows orange warning when alta_exposicao is true"
    - "SpouseDisclosure shows violet icon + 'via cônjuge' text with tooltip"
    - "QsaEmptyState renders with correct heading 'Nenhuma relação encontrada'"
    - "QsaErrorState renders error message with retry button"
    - "QsaFreshnessBanner renders 4 distinct states (green/amber/red/offline)"
    - "All 7 components exported from fiscalizacao/index.tsx"
  artifacts:
    - path: "Frontend/components/fiscalizacao/ConflictBadge.tsx"
      provides: "Conflict badge with tooltip"
      contains: "Conflito"
    - path: "Frontend/components/fiscalizacao/QsaFreshnessBanner.tsx"
      provides: "Staleness indicator banner"
      contains: "Dados atualizados"
    - path: "Frontend/components/fiscalizacao/index.tsx"
      provides: "Barrel exports for all 7 new components + 5 existing"
      contains: "QsaFreshnessBanner"
  key_links:
    - from: "ConflictBadge.tsx"
      to: "@/components/ui/tooltip"
      via: "Tooltip, TooltipContent, TooltipTrigger imports"
    - from: "index.tsx"
      to: "./ConflictBadge"
      via: "export { ConflictBadge }"
    - from: "QsaFreshnessBanner.tsx"
      to: "lucide-react"
      via: "CheckCircle2, AlertTriangle, AlertCircle icons"
</interfaces>
---

<objective>
Create all reusable QSA display components (badges, indicators, states) that the dashboard page and profile inline section will compose together. These are pure presentational components — no data fetching, no URL state.

**Purpose:** Build the visual vocabulary for the QSA dashboard. Every badge, flag, and state screen is defined here as independently testable components that Plan 03 and 04 will import and compose into pages.

**Output:** 7 new domain components in `components/fiscalizacao/`, 1 shadcn Tooltip wrapper, updated barrel index
</objective>

<execution_context>
@C:/Users/AndreC/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/AndreC/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-qsa-dashboard-core/05-CONTEXT.md — Locked decisions D-13, D-14, D-15
@.planning/phases/05-qsa-dashboard-core/05-UI-SPEC.md — Color palette (§Colors), Copywriting contract (§Copywriting), component spec (§Component Inventory), states (§States Specification)
@.planning/phases/05-qsa-dashboard-core/05-RESEARCH.md — ConflictBadge code example, Freshness Banner pattern, MatchTypeBadge design notes

<interfaces>
From Frontend/services/api.ts — types that components receive as props:
```typescript
export interface RelacaoDetalhada {
  id: number;
  cnpj: string;
  tipo_relacao: string;
  relationship_type: boolean | null;
  score_confianca: number;
  score_conflito: number;
  conflito_interesse: boolean;
  alta_exposicao: boolean;
  via_conjuge: boolean;
  empresa: {
    razao_social: string;
    municipio: string | null;
    capital_social: number | null;
    cnae_principal: string | null;
    cnae_descricao: string | null;
  };
}

export interface QsaFreshness {
  qsa_data_disponivel: boolean;
  ultima_atualizacao_qsa?: string;
  dias_desde_atualizacao?: number;
  dados_antigos?: boolean;
}
```

From RiscoScore.tsx — existing badge pattern to replicate (lines 16-54):
- CONFIG map with cor (text-{color}-400), bg (bg-{color}-950/40), borda (border-{color}-800)
- lucide-react icons for flags
- cn() from @/lib/utils for conditional classes

Barrel file: Frontend/components/fiscalizacao/index.tsx — currently exports 5 components:
```typescript
export { FiscalizacaoSection } from "./FiscalizacaoSection";
export { RiscoScore } from "./RiscoScore";
export { AlertasLista } from "./AlertList";
export { PatrimonioChart } from "./PatrimonioChart";
export { CEAPDashboard } from "./CEAPDashboard";
```

UI-SPEC color tokens for badges (spec section §Colors - Semantic Colors):
- No conflict: emerald-400/950/800 | Low conflict: amber-400/950/800 | High conflict: red-400/950/800
- Exposure: orange-400/950/800 | CPF match: emerald-400 | Nome match: amber-400
- Spouse: violet-400 | Freshness green: emerald-400 | Freshness amber: amber-400 | Freshness red: red-400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn Tooltip component</name>
  <read_first>
    Frontend/components/ui/button.tsx — shadcn component pattern (structure, imports from @/components/ui)
    Frontend/components/ui/skeleton.tsx — simpler shadcn pattern for reference
    Frontend/components.json — shadcn config for style resolution
  </read_first>
  <files>
    Frontend/components/ui/tooltip.tsx
  </files>
  <action>
    Run `npx shadcn@latest add tooltip -y` to generate the Tooltip wrapper component at `Frontend/components/ui/tooltip.tsx`. If the CLI command fails or isn't available, create a manual wrapper following the same shadcn pattern as `button.tsx` and `skeleton.tsx`: import from `radix-ui` (Tooltip.Provider, Tooltip.Root, Tooltip.Trigger, Tooltip.Content, Tooltip.Portal), use `cn()` for conditional classes, match the dark-mode styling conventions. The component must export `TooltipProvider`, `Tooltip`, `TooltipTrigger`, `TooltipContent` — matching the shadcn API that downstream docs show (TooltipProvider wraps root; Tooltip → Tooltip.Root; TooltipTrigger → Tooltip.Trigger; TooltipContent → Tooltip.Portal + Tooltip.Content).
  </action>
  <acceptance_criteria>
    1. File `Frontend/components/ui/tooltip.tsx` exists
    2. Exports: `TooltipProvider`, `Tooltip`, `TooltipTrigger`, `TooltipContent`
    3. `npx tsc --noEmit` passes without errors from components importing tooltip
    4. Component uses `cn()` utility for className merging
    5. TooltipContent has appropriate dark-mode styling (bg-gray-900 border border-gray-700 text-gray-100)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build ConflictBadge, MatchTypeBadge, ExposureIndicator, SpouseDisclosure</name>
  <read_first>
    Frontend/components/fiscalizacao/RiscoScore.tsx — Color/styling pattern (cn(), lucide icons, bg-*-950/40 border-*-800)
    Frontend/components/ui/tooltip.tsx — TooltipProvider, Tooltip, TooltipTrigger, TooltipContent
    .planning/phases/05-qsa-dashboard-core/05-UI-SPEC.md — Badge colors, tooltip copy, badge background pattern (§Badge Background Pattern)
  </read_first>
  <files>
    Frontend/components/fiscalizacao/ConflictBadge.tsx
    Frontend/components/fiscalizacao/MatchTypeBadge.tsx
    Frontend/components/fiscalizacao/ExposureIndicator.tsx
    Frontend/components/fiscalizacao/SpouseDisclosure.tsx
  </files>
  <action>
    Create four pure display components. All follow the established badge pattern: `bg-{color}-950/40 border border-{color}-800 rounded-full px-2.5 py-1 text-xs font-medium inline-flex items-center gap-1.5`.

    **ConflictBadge.tsx** — Props: `{ hasConflict: boolean; score?: number }`. When `hasConflict=true`: red dot (`bg-red-400 w-2 h-2 rounded-full`) + "Conflito" label (red-400 text). When `hasConflict=false`: green dot (`bg-emerald-400`) + "Sem conflito" label (emerald-400 text). Wrap in `<Tooltip>` (using shadcn Tooltip wrapper from Task 1, wrapped in `<TooltipProvider>` only if standalone usage — preferably export a wrapped version that works without provider context): when `hasConflict && score !== undefined`, show tooltip with "Score de conflito: {score}/100". Use `cn()` for conditional colors. Icons: not needed for this component.

    **MatchTypeBadge.tsx** — Props: `{ relationshipType: boolean | null }`. When `relationshipType=false` (CPF match): green badge "CPF" with tooltip "Correspondência exata por CPF" using emerald-400. When `relationshipType=true` (Nome match): amber badge "Nome" with tooltip "Correspondência por nome do cônjuge" using amber-400. When `null`: render nothing (return null). Use lucide `UserCheck` icon for CPF, `UserSearch` for Nome.

    **ExposureIndicator.tsx** — Props: `{ altaExposicao: boolean; capitalSocial?: number | null }`. When `altaExposicao=true`: render as a `span` with orange-400 text + `AlertTriangle` lucide icon (w-3.5 h-3.5) + formatted capital value `R$ {capitalSocial.toLocaleString('pt-BR')}`. When `altaExposicao=false`: render as muted gray text with capital value only (no icon, gray-500). Wrap in Tooltip with "Alta exposição financeira" / "Capital social superior a R$ 1.000.000,00" text. Apply inline-flex pattern to keep it aligned.

    **SpouseDisclosure.tsx** — Props: `{ viaConjuge: boolean }`. When `viaConjuge=true`: render violet-400 badge with `Heart` lucide icon (w-3.5 h-3.5) + "via cônjuge" text. Wrapped in Tooltip with "Relação vinculada através do cônjuge do deputado". When `viaConjuge=false`: render nothing (return null).
  </action>
  <acceptance_criteria>
    1. All four files exist and export named components
    2. `npx tsc --noEmit` passes with no type errors
    3. ConflictBadge renders red/Conflito for `hasConflict=true`, green/Sem conflito for `false`
    4. MatchTypeBadge renders green/CPF for `relationship_type=false`, amber/Nome for `true`, nothing for `null`
    5. ExposureIndicator shows orange indicator + BRL formatted capital when `altaExposicao=true`
    6. SpouseDisclosure shows violet badge with "via cônjuge" when `viaConjuge=true`, renders nothing when `false`
    7. Tooltips appear on hover for ConflictBadge (with score), MatchTypeBadge, ExposureIndicator, SpouseDisclosure
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build QsaFreshnessBanner, QsaEmptyState, QsaErrorState + update barrel</name>
  <read_first>
    Frontend/components/fiscalizacao/RiscoScore.tsx — cn() pattern
    Frontend/services/api.ts — QsaFreshness type (lines 222-227)
    .planning/phases/05-qsa-dashboard-core/05-UI-SPEC.md — Freshness states table (§States Specification > QsaFreshnessBanner), empty/error state copy, icons
    Frontend/components/fiscalizacao/index.tsx — Current exports to extend
  </read_first>
  <files>
    Frontend/components/fiscalizacao/QsaFreshnessBanner.tsx
    Frontend/components/fiscalizacao/QsaEmptyState.tsx
    Frontend/components/fiscalizacao/QsaErrorState.tsx
    Frontend/components/fiscalizacao/index.tsx
  </files>
  <action>
    Create three state display components and update the barrel.

    **QsaFreshnessBanner.tsx** — Props: `{ freshness?: QsaFreshness | null; loading?: boolean }`.
    Implements 4 visual states per UI-SPEC:
    - LOADING (`loading=true` OR no props): render `<Skeleton className="h-10 rounded-lg" />` from `@/components/ui/skeleton`
    - GREEN (`qsa_data_disponivel=true` and `dias_desde_atualizacao <= 30` and `dados_antigos != true`): `bg-emerald-950/20 border border-emerald-800 rounded-lg p-3 flex items-center gap-2`. Icon: `CheckCircle2` (emerald-400, w-4 h-4). Text: "Dados atualizados ({dias} dias atrás)" in emerald-400.
    - AMBER (`dias_desde_atualizacao > 30` and `<= 45`): `bg-amber-950/20 border-amber-800`. Icon: `AlertTriangle` (amber-400). Text: "Dados com {dias} dias" in amber-400.
    - RED (`dias_desde_atualizacao > 45` or `dados_antigos == true`): `bg-red-950/20 border-red-800`. Icon: `AlertTriangle` (red-400). Text: "Dados desatualizados ({dias} dias)" in red-400.
    - OFFLINE (`qsa_data_disponivel == false`): `bg-gray-800 border-gray-700`. Icon: `AlertCircle` (gray-400). Text: "Dados QSA indisponíveis" in gray-400.
    Use `text-sm font-medium` for the message text. All icons from `lucide-react`.

    **QsaEmptyState.tsx** — Props: `{ variant?: "filters" | "nodata" }`.
    Centered div with `py-16 text-center`:
    - `variant="filters"` (default): `SearchX` icon (w-12 h-12, gray-600, mx-auto mb-4), heading "Nenhuma relação encontrada" (text-gray-400 text-lg font-semibold), body "Tente alterar os filtros para ver mais resultados." (text-gray-500 text-sm).
    - `variant="nodata"`: `Building2` icon, same heading, body "Este deputado não possui vínculos QSA com empresas."

    **QsaErrorState.tsx** — Props: `{ onRetry?: () => void; variant?: "page" | "expand" }`.
    Centered div with `py-16 text-center`:
    - `variant="page"` (default): `AlertCircle` icon (w-12 h-12, red-400/80, mx-auto mb-4), heading "Erro ao carregar" (text-gray-400 text-lg font-semibold), body "Não foi possível carregar os dados. Verifique sua conexão e tente novamente." (text-gray-500 text-sm, mb-6), retry `<Button variant="outline" onClick={onRetry}>Tentar novamente</Button>` from `@/components/ui/button`.
    - `variant="expand"`: heading "Erro ao carregar relações", body "Não foi possível carregar as relações deste deputado.", inline retry `<button className="text-xs text-brasil-amarelo hover:underline">Tentar novamente</button>` (not a Button, styled inline link).

    **Barrel update (index.tsx):** Add export lines at the end of the existing file:
    ```
    export { ConflictBadge } from "./ConflictBadge";
    export { MatchTypeBadge } from "./MatchTypeBadge";
    export { ExposureIndicator } from "./ExposureIndicator";
    export { SpouseDisclosure } from "./SpouseDisclosure";
    export { QsaFreshnessBanner } from "./QsaFreshnessBanner";
    export { QsaEmptyState } from "./QsaEmptyState";
    export { QsaErrorState } from "./QsaErrorState";
    ```
    Keep all 5 existing exports unchanged.
  </action>
  <acceptance_criteria>
    1. All three state component files exist and export named components
    2. `npx tsc --noEmit` passes with no type errors
    3. QsaFreshnessBanner renders 4 distinct color states based on freshness data (testable by passing different props)
    4. QsaEmptyState renders correct heading "Nenhuma relação encontrada" for both variants
    5. QsaErrorState renders "Erro ao carregar" heading with retry button for page variant, inline retry for expand variant
    6. Barrel file exports all 7 new components plus 5 existing ones (12 total exports)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component props | Data flows from parent (API data) into display components; no user input accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | Tooltip content (score numbers) | accept | Score data is public transparency data; no PII in tooltips |
| T-05-04 | Tampering | `relationship_type` null handling | mitigate | MatchTypeBadge returns null (no render) when relationship_type is null — prevents undefined access errors |
| T-05-SC | Tampering | npm install (tooltip via shadcn) | accept | shadcn official registry only; Tooltip comes from `radix-ui@1.4.3` already in package.json |
</threat_model>

<verification>
1. `cd Frontend && npx tsc --noEmit` passes with zero errors
2. `cd Frontend && npm run build` succeeds (production build)
3. Each component can be rendered in isolation without crashing (manual verification against UI-SPEC)
</verification>

<success_criteria>
1. All 7 display components exist and compile without type errors
2. Tooltip wrapper is available for downstream usage
3. Barrel exports include all 7 new components alongside 5 existing ones
4. Components use correct semantic colors matching UI-SPEC
5. Copywriting matches pt-BR strings from UI-SPEC verbatim
</success_criteria>
